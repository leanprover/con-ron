/-!
The `_bridge` convention: DESIGN §8.2's own name for the join point, with
one per-constructor refinement, and the fold's capstone.
-/

namespace ConRon.Bridge

theorem checkDecl_bridge_defn (pd : IDeclaration) : True := by
  sorry

theorem Arena.checkDeclsPure_bridge (ds : List IDeclaration) : True := by
  trivial

end ConRon.Bridge
