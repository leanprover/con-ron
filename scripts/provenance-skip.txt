# scripts/provenance-skip.txt — THE DELIBERATE SKIPS (task #33, DESIGN.md §3.7)
#
# The ledger (`scripts/provenance.py coverage`, and through it
# `scripts/progress.py`) counts a con-leche declaration as *to be ported*
# unless it is listed here.  One line per declaration:
#
#     <con-leche-relative Lean path> <declaration> <reason>
#
# `<declaration>` may be `*`, which covers every declaration of that file.
# The reason is the rest of the line and is MANDATORY: a skip with no reason
# is a hole in the ledger, and `coverage` reports it as MALFORMED.  `coverage`
# also reports a skip that names no declaration of the file (STALE) and one
# whose declaration *is* cited after all (REDUNDANT), so the list cannot rot
# quietly.  Whatever is not here and not cited shows up as `uncovered`.
#
# This file is the machine-readable form of the task log's "Deliberately not
# ported" sections; every entry's long argument is in the Rust module note of
# the module that would have held it.  It does NOT affect
# `scripts/provenance.py check`, which is about the Rust side.

# ---------------------------------------------------------------------------
# Elaboration-time meta code: the Rust core has no elaborator (DESIGN.md §4)
# ---------------------------------------------------------------------------

ConLeche/Kernel/BasisGen.lean * The `#annotate_basis`/`#annotate_pins` elaborators and their `Qq` quotations — meta code that runs while con-leche elaborates; its *results* are carried as generated source (`kernel/basis_tables.rs`, task #22) and as the annotated pins in `std_axioms`/`trust_axioms`.
ConLeche/Kernel/TrustAxioms.lean trustPinEnv The private environment `#annotate_pins` is run over while `TrustAxioms.lean` elaborates; what the port carries is the annotated pins that command produces, which `trust_axioms.rs` holds.

# ---------------------------------------------------------------------------
# Proof-tier and mode-gated variants of bodies the port has once (§3.1)
# ---------------------------------------------------------------------------

ConLeche/Kernel/CoreGated.lean * The gated (proof-tier) variants of `Kernel/Core.lean`'s bodies and knot: not executed by the shipped path, and the port has one knot, the cached one (`cached::core_c`).
ConLeche/Kernel/CheckerGated.lean * The gated `CheckerOps` instantiations (`fueledOpsGated`, `pureOpsGated`): the proof tier's variants of `fueledOps`/`pureOps`, which `kernel::type_checker` has once.
ConLeche/Kernel/CoreIO.lean * The io-grade knot variant: the port's `infer_at_i` carries the io grade as a runtime flag (task #18's deviation 4), so `coreKnotIO`/`pureFnsIO`/`inferTypeCoreIO` have no second spelling.
ConLeche/Kernel/TypeChecker.lean pureFns The PURE knot at `CheckM`; §3.1 gives the port one knot — the cached one it executes (`cached::core_c`) — and `kernel/type_checker.rs`'s module note records the deviation, with every refinement lemma stated against `coreKnotI`.
ConLeche/Cached/CoreC.lean whnfCoreBodyPC One cached body instantiated at a literal `CheckMode`; the port's mode is a runtime `&CheckMode` parameter, so `whnf_core_body_i(&CheckMode::Verified, …)` IS this definition (`cached/core_c.rs`'s closing note).
ConLeche/Cached/CoreC.lean inferBodyPC As `whnfCoreBodyPC`: `infer_body_i` at `CheckMode::Verified`.
ConLeche/Cached/CoreC.lean defeqBodyPC As `whnfCoreBodyPC`: `defeq_body_i` at `CheckMode::Verified`.
ConLeche/Cached/CoreC.lean annotateBodyPC As `whnfCoreBodyPC`: `annotate_body_i`, which reads no mode at all (hence no `TC` sibling).
ConLeche/Cached/CoreC.lean whnfCoreBodyTC As `whnfCoreBodyPC`: `whnf_core_body_i` at `CheckMode::Trusted`.
ConLeche/Cached/CoreC.lean inferBodyTC As `whnfCoreBodyPC`: `infer_body_i` at `CheckMode::Trusted`.
ConLeche/Cached/CoreC.lean defeqBodyTC As `whnfCoreBodyPC`: `defeq_body_i` at `CheckMode::Trusted`.

# ---------------------------------------------------------------------------
# `Prop`s and proof-carrying apparatus: Rust has no `Prop` (§3.5)
# ---------------------------------------------------------------------------

ConLeche/Kernel/ExprOps.lean Inst1MemoInv A memo invariant, `Prop`-valued ("every recorded answer is the real one"): the specification the port's memo is proved against, not code.
ConLeche/Kernel/ExprOps.lean InstLMemoInv A `Prop`-valued memo invariant, as `Inst1MemoInv`.
ConLeche/Kernel/ExprOps.lean LiftMemoInv A `Prop`-valued memo invariant, as `Inst1MemoInv`.
ConLeche/Kernel/ExprOps.lean ResetMemoInv A `Prop`-valued memo invariant, as `Inst1MemoInv`.
ConLeche/Kernel/ExprOps.lean RenameMemoInv A `Prop`-valued memo invariant, as `Inst1MemoInv`.
ConLeche/Kernel/ExprOps.lean MemoBInv A `Prop`-valued memo invariant, as `Inst1MemoInv`.
ConLeche/Kernel/ExprOps.lean MemoFInv A `Prop`-valued memo invariant, as `Inst1MemoInv`.
ConLeche/Kernel/ExprOps.lean Abs1MemoInv A `Prop`-valued memo invariant, as `Inst1MemoInv`.
ConLeche/Kernel/ExprOps.lean LowerMemoInv A `Prop`-valued memo invariant, as `Inst1MemoInv`.
ConLeche/Kernel/ExprOps.lean Inst1LMemoInv A `Prop`-valued memo invariant, as `Inst1MemoInv`.
ConLeche/Kernel/ExprOps.lean ILPMemoInv A `Prop`-valued memo invariant, as `Inst1MemoInv`.
ConLeche/Kernel/Level.lean LPMemoInv A `Prop`-valued memo invariant, as `ExprOps.lean`'s.
ConLeche/Kernel/DeclCheck.lean CRFMemoInv A `Prop`-valued memo invariant, as `ExprOps.lean`'s.
ConLeche/Kernel/Inductives/NativeInstall.lean MentionsFvarMemoInv A `Prop`-valued memo invariant, as `ExprOps.lean`'s.
ConLeche/Kernel/Inductives/StructParts.lean LooseBVarMemoInv A `Prop`-valued memo invariant, as `ExprOps.lean`'s.
ConLeche/Kernel/Inductives/StructParts.lean MentionsMemoInv A `Prop`-valued memo invariant, as `ExprOps.lean`'s.
ConLeche/Kernel/PropWhen.lean casesZ The registered `cases`/`induction` eliminator of `PropWhen` — an elaboration-time device for writing proofs against the two-constructor view, with no run-time content.
ConLeche/Cached/Installed.lean InstallRun Phase A's accepting run, an `inductive … : Prop`; the parallel driver's evidence plumbing (`cached/installed.rs`'s module note), whose verdict is `check_pending_list`, which IS ported.
ConLeche/Cached/Installed.lean InstalledEnv A structure indexed by an `InstallRun` proof; evidence plumbing, as `InstallRun`.
ConLeche/Cached/Installed.lean InstalledEnv.env The `.env` projection of that proof-carrying structure.
ConLeche/Cached/Installed.lean GroupChecked "Record `i` has been checked", a `Prop`; evidence plumbing, as `InstallRun`.
ConLeche/Cached/Installed.lean FullyChecked A subtype over `GroupChecked`; evidence plumbing, as `InstallRun`.
ConLeche/Cached/Installed.lean FullyChecked.assemble Builds that subtype from the proofs; evidence plumbing, as `InstallRun`.
ConLeche/Cached/Installed.lean FullyChecked.env The `.env` projection of that subtype.
ConLeche/Cached/Installed.lean checkRecord Runs `checkPending` and throws its value away for a `PLift (GroupChecked …)`; the ported `check_pending` is the whole run-time content.
ConLeche/Cached/Installed.lean CheckedRecord `{ k : Nat // GroupChecked … }` — a `Nat` at run time, a proof at compile time; evidence plumbing, as `InstallRun`.
ConLeche/Cached/Installed.lean RecordResult A worker's `Except (CheckError × Nat) (CheckedRecord …)`; evidence plumbing, as `InstallRun`.
ConLeche/Cached/Installed.lean checkRecordResult `checkRecord` tagged as a worker result; evidence plumbing, as `InstallRun`.

# ---------------------------------------------------------------------------
# The address-keyed apparatus of `Expr.beq`, superseded by task #30's memo
# (DESIGN.md §3.2; the long argument is `kernel/expr.rs`'s closing comment)
# ---------------------------------------------------------------------------

ConLeche/Kernel/Expr.lean EqPair.dflt The default a probe reads on a miss; the port's `probe_hit` reads an `Option` from `ron::HashMap`, so it needs no default.
ConLeche/Kernel/Expr.lean BeqResX The raw result of one node's comparison, carrying a `Decidable (a = b)`; the port returns a `bool` and its decision is proved, not carried.  (`beqBudget`, `BeqRes` and `BeqOut` were the pre-#319 spelling and are gone upstream: the budget went with the walks' at con-leche's task #317/#319 ruling, and this port never had it — `beq` runs the identity and computed-word guards BEFORE it allocates, so there was nothing left for a budget to buy, task #30.)
ConLeche/Kernel/Expr.lean BeqOutX `Squash (BeqResX …)` — the subsingleton wrapper that makes `withPtrAddr`'s side condition discharge; a `bool` here.
ConLeche/Kernel/Expr.lean BeqOutX.mk The constructor of that wrapper.
ConLeche/Kernel/Expr.lean BeqMap.record The write-back into an `Option BeqMap` absent until the first recorded pair; the port's table is allocated by `beq` itself behind its two guards, so `beq_record` writes into a table that always exists.
ConLeche/Kernel/Expr.lean withAddr `withPtrAddr` into a subsingleton — an address primitive Aeneas cannot model; `expr::ptr_eq` (§3.2's `Rc` axiom) is what the port has instead.
ConLeche/Kernel/Expr.lean ptrDec Identity decided by the pointer test; `expr::ptr_eq` is both this and `withAddr`.

# ---------------------------------------------------------------------------
# Driver-only rendering: `String`-valued, on no verdict path (§3.1 — the
# theorem never reads a message, so messages need not match)
# ---------------------------------------------------------------------------

ConLeche/Kernel/Name.lean toString A `Name`'s display string, for messages and the driver's output.
ConLeche/Kernel/PropWhen.lean reprPrec' The `Repr` instance's renderer, kept byte-identical for `#eval (repr …)` of a stored pin.

# ---------------------------------------------------------------------------
# Bridges to the Lean host, and one declaration nothing reads
# ---------------------------------------------------------------------------

ConLeche/Kernel/Name.lean ofLeanName Conversion from `Lean.Name`, used by con-leche's elaboration-time code (the pins, the tests); the Rust core has no `Lean.Name`.

# ---------------------------------------------------------------------------
# The cherries' residue (§5 P4.1; tasks #37, #39, #40).  Everything else of
# `ConLeche/Frontend/**` and `Main.lean` is ported, so with these entries the
# cherries ledger closes at 100 % of what is to be ported.
# ---------------------------------------------------------------------------

# The checker's own ANNOTATED NDJSON writer and the debug splice built on it:
# output paths (`lake exe con-leche-annot` writes the `tests/annot` fixtures,
# `CON_LECHE_INMODEL_DUMP` a copy of the input with the generated records
# spliced in), not something a checker reads.  con-ron reads those fixtures
# like any other stream; `crates/con-ron/src/lib.rs`'s module note has the
# argument (task #37 for the writer, task #39 for the splice).
ConLeche/Frontend/ExportWrite.lean * An output format, not a checking path: the checker's own annotated-NDJSON writer, which con-ron has no counterpart of (it reads the `tests/annot` fixtures it produces like any other stream).
ConLeche/Frontend/InModelDump.lean * `CON_LECHE_INMODEL_DUMP`'s debug splice, written through `ExportWrite.lean`'s writer: the same output path, one layer up (`in_model`'s module note).

# `Scan/Naive.lean` is the reference recogniser: the SPECIFICATION every item
# of `frontend::scan_fast` cites beside the `Fast.lean` declaration it ports
# (task #37, and P4.3's optional refinement).  What is skipped is its
# reference-only half — the reader monad it is written in, and the field
# tables `Fast.lean` inlines into its per-object slot loops, which the Rust
# recogniser therefore has no declaration for either.  `naiveObjLoop` and
# `naiveListLoop`, the two the port DOES factor out, are cited.
ConLeche/Frontend/Scan/Naive.lean NRes The reference recogniser's reader-monad result (remaining input plus value); `scan_fast` threads an index into a byte slice, so there is no monad to port.
ConLeche/Frontend/Scan/Naive.lean NRes.rest The `rest` projection of that result.
ConLeche/Frontend/Scan/Naive.lean NRes.map The `map` of that result.
ConLeche/Frontend/Scan/Naive.lean naiveObject The generic object reader over `NRes`; `Fast.lean` writes one slot loop per object and `scan_fast` ports those (`next_member`, `scan_obj_list_loop`).
ConLeche/Frontend/Scan/Naive.lean strNameFields A field table of the reference reader, inlined into `Fast.lean`'s slot loop and so into `scan_fast`'s.
ConLeche/Frontend/Scan/Naive.lean numNameFields A field table, as `strNameFields`.
ConLeche/Frontend/Scan/Naive.lean appFields A field table, as `strNameFields`.
ConLeche/Frontend/Scan/Naive.lean binderFields A field table, as `strNameFields`.
ConLeche/Frontend/Scan/Naive.lean letFields A field table, as `strNameFields`.
ConLeche/Frontend/Scan/Naive.lean constFields A field table, as `strNameFields`.
ConLeche/Frontend/Scan/Naive.lean projFields A field table, as `strNameFields`.
ConLeche/Frontend/Scan/Naive.lean ruleFields A field table, as `strNameFields`.
ConLeche/Frontend/Scan/Naive.lean indRecFields A field table, as `strNameFields`.
ConLeche/Frontend/Scan/Naive.lean indTypeFields A field table, as `strNameFields`.
ConLeche/Frontend/Scan/Naive.lean indCtorFields A field table, as `strNameFields`.
ConLeche/Frontend/Scan/Naive.lean DeclFields The record-shape table of a declaration line, as `strNameFields`.
ConLeche/Frontend/Scan/Naive.lean cvSlots The `ConstantVal` slot table, as `strNameFields`.
ConLeche/Frontend/Scan/Naive.lean axiomFields A field table, as `strNameFields`.
ConLeche/Frontend/Scan/Naive.lean defFields A field table, as `strNameFields`.
ConLeche/Frontend/Scan/Naive.lean thmFields A field table, as `strNameFields`.
ConLeche/Frontend/Scan/Naive.lean opaqueFields A field table, as `strNameFields`.
ConLeche/Frontend/Scan/Naive.lean quotFields A field table, as `strNameFields`.
ConLeche/Frontend/Scan/Naive.lean indFields A field table, as `strNameFields`.

# `Prepare.lean`'s two `List`-shaped SPECIFICATIONS of the preparation's
# picking, which con-leche proves its `Array` versions equal to.  Nothing on
# the run path calls either, and their `cons` is `Vec::insert`, which task #50
# took out of the port's vocabulary for good reason.  What the port does have
# is the specification as an executable check: `prepare.rs`'s test module
# carries `pick_spec` and runs `pick_idx` against it on every `cargo test`
# (task #84).
ConLeche/Frontend/Prepare.lean pickSpec The `List` specification of `pick`, which con-leche proves `pick` equal to; no caller on the run path, and its `d :: ds'` is `Vec::insert` (task #50). `prepare.rs`'s test module carries it as an executable check of `pick_idx`.
ConLeche/Frontend/Prepare.lean frontSpec The `List` specification of `frontOf`, as `pickSpec`.

# ---------------------------------------------------------------------------
# The walks' plain descents, child steps and proof carriers (task #98,
# con-leche's #317/#319: ONE memo discipline for every traversal memo)
# ---------------------------------------------------------------------------
#
# Upstream now writes each walk three times: `<name>P`, the memo-free plain
# descent, which is the SPECIFICATION the memoised walk carries its proof
# against; `enter<X>P`, the child step (cutoff, compound test, exclusivity
# read); and `<name>XP`, the walk.  The port has ONE function per walk, which
# is what it cites: the child step's two tests are made in the walk's own
# match and by `ron::node::is_exclusive`, and the plain descent has no
# counterpart here at all, because the port's specification is
# `ConLeche.Expr`'s own function and `Verify/Cached/OpsC.lean` is the bridge
# (`Refine/ExprOpsC*.lean`'s module note).  The `Squash`/`PEnt`/`BEnt`
# machinery is Lean's proof plumbing: an entry that carries its own equation,
# a quotient that makes the memo unobservable, and the address key
# `withPtrAddr` gives it — where the port's entry is a value, its result is a
# `bool` or an `Expr`, and its key is the structural one this port has always
# used (`cached/expr_ops_c.rs`'s `MemoN` note).

ConLeche/Cached/ExprOpsC.lean isCompound The substitution walks' child-step test; the port's walks make it in their own match, which answers the five atom kinds on the spot (`cached/expr_ops_c.rs`, module note 2).
ConLeche/Cached/ExprOpsC.lean resTerm Reads the rebuilt term out of a walk's `Squash`; the port's walks return the term.
ConLeche/Cached/ExprOpsC.lean resBool The `Bool` twin of `resTerm`.
ConLeche/Cached/ExprOpsC.lean pkey The packed ADDRESS-and-cursor memo key; the port keys structurally (`expr_ops::ExprNatKey`), which is the key con-leche itself used before its task #316 and the only one the model can see.
ConLeche/Cached/ExprOpsC.lean PKey That key under a mixing hash instance.
ConLeche/Cached/ExprOpsC.lean PEnt A self-proving memo entry: the node, its rebuild, the cursor and the equation between them; the port's entry is the rebuilt term, and the equation is the `_refines` lemma.
ConLeche/Cached/ExprOpsC.lean PEnt.hit Validates such an entry by cursor and pointer identity; the port's `memo1_get` returns the stored value and `MemoInv` is what makes it right.
ConLeche/Cached/ExprOpsC.lean HTab The `Std.HashMap PKey (PEnt s)` behind `MemoXP`; the port's is `ron::HashMap` at the structural key.
ConLeche/Cached/ExprOpsC.lean ResXP A walk's result: the term fixed by its proof, beside the memo; the port returns `(Expr, MemoN)`.
ConLeche/Cached/ExprOpsC.lean ResXP0 The cursor-free instance of it.
ConLeche/Cached/ExprOpsC.lean BEnt The `Bool` walks' self-proving entry; as `PEnt`.
ConLeche/Cached/ExprOpsC.lean BEnt.hit Validates one; as `PEnt.hit`.
ConLeche/Cached/ExprOpsC.lean BTab The `Bool` walks' table; as `HTab`.
ConLeche/Cached/ExprOpsC.lean ResB A `Bool` walk's result; as `ResXP`.
ConLeche/Cached/ExprOpsC.lean ResB0 The cursor-free instance of it.
ConLeche/Cached/ExprOpsC.lean instantiate1P The plain descent `instantiate1XP` carries its proof against; the port's specification is `ConLeche.Expr.instantiate1` itself.
ConLeche/Cached/ExprOpsC.lean instantiate1LiftP As `instantiate1P`, for the capture-avoiding substitution.
ConLeche/Cached/ExprOpsC.lean instantiateListP As `instantiate1P`, for the bulk substitution.
ConLeche/Cached/ExprOpsC.lean instantiateRevP As `instantiate1P`, on the reversed accumulator array.
ConLeche/Cached/ExprOpsC.lean abstract1P As `instantiate1P`, for the single abstraction.
ConLeche/Cached/ExprOpsC.lean abstractRangeP As `instantiate1P`, for the range abstraction.
ConLeche/Cached/ExprOpsC.lean instLevelParamsP As `instantiate1P`, for level instantiation.
ConLeche/Cached/ExprOpsC.lean wscopedBP As `instantiate1P`, for the scope guard.
ConLeche/Cached/ExprOpsC.lean leavesSubP As `instantiate1P`, for the leaf-subset guard.
ConLeche/Cached/ExprOpsC.lean allLevelParamsDefinedP As `instantiate1P`, for the definedness guard.
ConLeche/Cached/ExprOpsC.lean enter1P The child step of `instantiate1XP`; the port's walk makes its cutoff and compound test itself and reads `ron::node::is_exclusive` where this reads `withExclusive`.
ConLeche/Cached/ExprOpsC.lean enterLiftP As `enter1P`.
ConLeche/Cached/ExprOpsC.lean enterListP As `enter1P`.
ConLeche/Cached/ExprOpsC.lean enterRevP As `enter1P`.
ConLeche/Cached/ExprOpsC.lean enterAbs1P As `enter1P`.
ConLeche/Cached/ExprOpsC.lean enterAbsRP As `enter1P`.
ConLeche/Cached/ExprOpsC.lean enterLPP As `enter1P`, with no compound test (a `const` with level parameters is worth recording) — the port's `inst_level_params_go` likewise gates on the read alone.
ConLeche/Cached/ExprOpsC.lean enterLSub As `enter1P`, through `expr_ops_c::memo_skip`.
ConLeche/Cached/ExprOpsC.lean enterLPD As `enter1P`, through `expr_ops_c::memo_skip`.
ConLeche/Cached/ExprOpsC.lean leavesSubC The cutoff-and-walk wrapper of the leaf-subset guard; the port has only its one caller, `leaf_guard`, which is what the cited `leafGuard` calls (`cached/expr_ops_c.rs`'s `leaves_sub_go` note).
ConLeche/Cached/StateC.lean constsResolveFP The plain descent `constsResolveFXP` carries its proof against; as `instantiate1P`.
ConLeche/Cached/StateC.lean enterCRF The child step of `constsResolveFXP`; as `enter1P`, through `expr_ops_c::memo_skip`.
ConLeche/Kernel/Exclusive.lean withExclusiveUnsafe The `@[implemented_by]` half of `withExclusive`: `k (isExclusiveUnsafe a)`, Lean's own `unsafe` count read.  The port's `ron::node::is_exclusive` IS that read, and it cites `withExclusive`, the logical half.
ConLeche/Kernel/Exclusive.lean withExcl `withExclusive` into a subsingleton, so that the obligation `k true = k false` is `Subsingleton.elim`.  The port has no obligation to discharge: `is_exclusive` is an external hole modelled `ok false`, and OVERVIEW §8.1's row is what stands where the obligation stands here.
