# scripts/provenance-skip.txt — THE DELIBERATE SKIPS (task #33, DESIGN.md §3.7)
#
# The ledger (`scripts/provenance.py coverage`, and through it
# `scripts/progress.py`) counts a con-leche declaration as *to be ported*
# unless it is listed here.  One line per declaration:
#
#     <con-leche-relative Lean path> <declaration> <reason>
#
# `<declaration>` may be `*`, which covers every declaration of that file.
# The reason is the rest of the line and is MANDATORY: a skip with no reason
# is a hole in the ledger, and `coverage` reports it as MALFORMED.  `coverage`
# also reports a skip that names no declaration of the file (STALE) and one
# whose declaration *is* cited after all (REDUNDANT), so the list cannot rot
# quietly.  Whatever is not here and not cited shows up as `uncovered`.
#
# This file is the machine-readable form of the task log's "Deliberately not
# ported" sections; every entry's long argument is in the Rust module note of
# the module that would have held it.  It does NOT affect
# `scripts/provenance.py check`, which is about the Rust side.

# ---------------------------------------------------------------------------
# Elaboration-time meta code: the Rust core has no elaborator (DESIGN.md §4)
# ---------------------------------------------------------------------------

ConLeche/Kernel/BasisGen.lean * The `#annotate_basis`/`#annotate_pins` elaborators and their `Qq` quotations — meta code that runs while con-leche elaborates; its *results* are carried as generated source (`kernel/basis_tables.rs`, task #22) and as the annotated pins in `std_axioms`/`trust_axioms`.
ConLeche/Kernel/TrustAxioms.lean trustPinEnv The private environment `#annotate_pins` is run over while `TrustAxioms.lean` elaborates; what the port carries is the annotated pins that command produces, which `trust_axioms.rs` holds.

# ---------------------------------------------------------------------------
# Proof-tier and mode-gated variants of bodies the port has once (§3.1)
# ---------------------------------------------------------------------------

ConLeche/Kernel/CoreGated.lean * The gated (proof-tier) variants of `Kernel/Core.lean`'s bodies and knot: not executed by the shipped path, and the port has one knot, the cached one (`cached::core_c`).
ConLeche/Kernel/CheckerGated.lean * The gated `CheckerOps` instantiations (`fueledOpsGated`, `pureOpsGated`): the proof tier's variants of `fueledOps`/`pureOps`, which `kernel::type_checker` has once.
ConLeche/Kernel/CoreIO.lean * The io-grade knot variant: the port's `infer_at_i` carries the io grade as a runtime flag (task #18's deviation 4), so `coreKnotIO`/`pureFnsIO`/`inferTypeCoreIO` have no second spelling.
ConLeche/Kernel/TypeChecker.lean pureFns The PURE knot at `CheckM`; §3.1 gives the port one knot — the cached one it executes (`cached::core_c`) — and `kernel/type_checker.rs`'s module note records the deviation, with every refinement lemma stated against `coreKnotI`.
ConLeche/Cached/CoreC.lean whnfCoreBodyPC One cached body instantiated at a literal `CheckMode`; the port's mode is a runtime `&CheckMode` parameter, so `whnf_core_body_i(&CheckMode::Verified, …)` IS this definition (`cached/core_c.rs`'s closing note).
ConLeche/Cached/CoreC.lean inferBodyPC As `whnfCoreBodyPC`: `infer_body_i` at `CheckMode::Verified`.
ConLeche/Cached/CoreC.lean defeqBodyPC As `whnfCoreBodyPC`: `defeq_body_i` at `CheckMode::Verified`.
ConLeche/Cached/CoreC.lean annotateBodyPC As `whnfCoreBodyPC`: `annotate_body_i`, which reads no mode at all (hence no `TC` sibling).
ConLeche/Cached/CoreC.lean whnfCoreBodyTC As `whnfCoreBodyPC`: `whnf_core_body_i` at `CheckMode::Trusted`.
ConLeche/Cached/CoreC.lean inferBodyTC As `whnfCoreBodyPC`: `infer_body_i` at `CheckMode::Trusted`.
ConLeche/Cached/CoreC.lean defeqBodyTC As `whnfCoreBodyPC`: `defeq_body_i` at `CheckMode::Trusted`.

# ---------------------------------------------------------------------------
# `Prop`s and proof-carrying apparatus: Rust has no `Prop` (§3.5)
# ---------------------------------------------------------------------------

ConLeche/Kernel/ExprOps.lean Inst1MemoInv A memo invariant, `Prop`-valued ("every recorded answer is the real one"): the specification the port's memo is proved against, not code.
ConLeche/Kernel/ExprOps.lean InstLMemoInv A `Prop`-valued memo invariant, as `Inst1MemoInv`.
ConLeche/Kernel/ExprOps.lean LiftMemoInv A `Prop`-valued memo invariant, as `Inst1MemoInv`.
ConLeche/Kernel/ExprOps.lean ResetMemoInv A `Prop`-valued memo invariant, as `Inst1MemoInv`.
ConLeche/Kernel/ExprOps.lean RenameMemoInv A `Prop`-valued memo invariant, as `Inst1MemoInv`.
ConLeche/Kernel/ExprOps.lean MemoBInv A `Prop`-valued memo invariant, as `Inst1MemoInv`.
ConLeche/Kernel/ExprOps.lean MemoFInv A `Prop`-valued memo invariant, as `Inst1MemoInv`.
ConLeche/Kernel/ExprOps.lean Abs1MemoInv A `Prop`-valued memo invariant, as `Inst1MemoInv`.
ConLeche/Kernel/ExprOps.lean LowerMemoInv A `Prop`-valued memo invariant, as `Inst1MemoInv`.
ConLeche/Kernel/ExprOps.lean Inst1LMemoInv A `Prop`-valued memo invariant, as `Inst1MemoInv`.
ConLeche/Kernel/ExprOps.lean ILPMemoInv A `Prop`-valued memo invariant, as `Inst1MemoInv`.
ConLeche/Kernel/Level.lean LPMemoInv A `Prop`-valued memo invariant, as `ExprOps.lean`'s.
ConLeche/Kernel/DeclCheck.lean CRFMemoInv A `Prop`-valued memo invariant, as `ExprOps.lean`'s.
ConLeche/Kernel/Inductives/NativeInstall.lean MentionsFvarMemoInv A `Prop`-valued memo invariant, as `ExprOps.lean`'s.
ConLeche/Kernel/Inductives/StructParts.lean LooseBVarMemoInv A `Prop`-valued memo invariant, as `ExprOps.lean`'s.
ConLeche/Kernel/Inductives/StructParts.lean MentionsMemoInv A `Prop`-valued memo invariant, as `ExprOps.lean`'s.
ConLeche/Kernel/PropWhen.lean casesZ The registered `cases`/`induction` eliminator of `PropWhen` — an elaboration-time device for writing proofs against the two-constructor view, with no run-time content.
ConLeche/Cached/Installed.lean InstallRun Phase A's accepting run, an `inductive … : Prop`; the parallel driver's evidence plumbing (`cached/installed.rs`'s module note), whose verdict is `check_pending_list`, which IS ported.
ConLeche/Cached/Installed.lean InstalledEnv A structure indexed by an `InstallRun` proof; evidence plumbing, as `InstallRun`.
ConLeche/Cached/Installed.lean InstalledEnv.env The `.env` projection of that proof-carrying structure.
ConLeche/Cached/Installed.lean GroupChecked "Record `i` has been checked", a `Prop`; evidence plumbing, as `InstallRun`.
ConLeche/Cached/Installed.lean FullyChecked A subtype over `GroupChecked`; evidence plumbing, as `InstallRun`.
ConLeche/Cached/Installed.lean FullyChecked.assemble Builds that subtype from the proofs; evidence plumbing, as `InstallRun`.
ConLeche/Cached/Installed.lean FullyChecked.env The `.env` projection of that subtype.
ConLeche/Cached/Installed.lean checkRecord Runs `checkPending` and throws its value away for a `PLift (GroupChecked …)`; the ported `check_pending` is the whole run-time content.
ConLeche/Cached/Installed.lean CheckedRecord `{ k : Nat // GroupChecked … }` — a `Nat` at run time, a proof at compile time; evidence plumbing, as `InstallRun`.
ConLeche/Cached/Installed.lean RecordResult A worker's `Except (CheckError × Nat) (CheckedRecord …)`; evidence plumbing, as `InstallRun`.
ConLeche/Cached/Installed.lean checkRecordResult `checkRecord` tagged as a worker result; evidence plumbing, as `InstallRun`.
ConLeche/Cached/Installed.lean collectChecks Assembles a table of those proofs in record order; its verdict is `checkPendingList`'s, which is ported as `check_pending_list`.

# ---------------------------------------------------------------------------
# The address-keyed apparatus of `Expr.beq`, superseded by task #30's memo
# (DESIGN.md §3.2; the long argument is `kernel/expr.rs`'s closing comment)
# ---------------------------------------------------------------------------

ConLeche/Kernel/Expr.lean EqPair.dflt The default a probe reads on a miss; the port's `probe_hit` reads an `Option` from `ron::HashMap`, so it needs no default.
ConLeche/Kernel/Expr.lean beqBudget The node budget before the memo table is materialised; the port's `beq` runs the identity and computed-word guards BEFORE it allocates, so the budget has nothing left to buy (task #30 measured it).
ConLeche/Kernel/Expr.lean BeqRes The raw result of one node's comparison, carrying a `Decidable (a = b)`; the port returns a `bool` and its decision is proved, not carried.
ConLeche/Kernel/Expr.lean BeqOut `Squash (BeqRes …)` — the subsingleton wrapper that makes `withPtrAddr`'s side condition discharge; a `bool` here.
ConLeche/Kernel/Expr.lean BeqOut.mk The constructor of that wrapper.
ConLeche/Kernel/Expr.lean withAddr `withPtrAddr` into a subsingleton — an address primitive Aeneas cannot model; `expr::ptr_eq` (§3.2's `Rc` axiom) is what the port has instead.
ConLeche/Kernel/Expr.lean ptrDec Identity decided by the pointer test; `expr::ptr_eq` is both this and `withAddr`.

# ---------------------------------------------------------------------------
# Driver-only rendering: `String`-valued, on no verdict path (§3.1 — the
# theorem never reads a message, so messages need not match)
# ---------------------------------------------------------------------------

ConLeche/Cached/ParsedC.lean msSecs Milliseconds rendered as `s.d` seconds for the driver's progress line.
ConLeche/Cached/ParsedC.lean declCLabel A parsed declaration's display label (`Main.declCName`'s shared spelling).
ConLeche/Kernel/CheckerSplit.lean ValueKind.word The kind's word ("definition"/"theorem"/"opaque") in `checkDecl`'s type-mismatch message.
ConLeche/Kernel/Checker.lean divModAttemptReason What a Nat-op pin variant failed on, for the decline message; the pure instantiations always report the `none` text.
ConLeche/Kernel/Name.lean toString A `Name`'s display string, for messages and the driver's output.
ConLeche/Kernel/PropWhen.lean reprPrec' The `Repr` instance's renderer, kept byte-identical for `#eval (repr …)` of a stored pin.

# ---------------------------------------------------------------------------
# Bridges to the Lean host, and one declaration nothing reads
# ---------------------------------------------------------------------------

ConLeche/Kernel/Name.lean ofLeanName Conversion from `Lean.Name`, used by con-leche's elaboration-time code (the pins, the tests); the Rust core has no `Lean.Name`.
ConLeche/Kernel/Basis.lean BasisKind.decls The RAW block dispatcher, which nothing in con-leche's implementation reads (a grep of `Kernel/` and `Cached/` finds no use): the installation stores the ANNOTATED blocks, `BasisKind.declsA`, which is `kernel::basis_tables::basis_decls_a`, and the individual raw pins are compared one by one in `std_axioms`/`trust_axioms`/`basis_pins`.
