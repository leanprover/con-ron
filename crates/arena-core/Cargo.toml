[package]
name = "arena-core"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "The arena stores of con-ron's rewrite (DESIGN.md §8.5, task #97 P4a)"

[lib]
path = "src/lib.rs"

[dependencies]
con-ron-core = { path = "../con-ron-core" }
