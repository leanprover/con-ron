# Expected exit codes for con-leche's own end-to-end fixtures, read by
# tests/arena.sh (0 accept, 1 reject, 2 decline, 3 error).  Line format:
# "<expectation> <fixture>" — ONE line per fixture.
#
# Until task #207 a row could carry a third field, `raw` (run with the
# preprocessor made unavailable) or `pre` (run with --pre, skipping the
# spawn), and a fixture could appear twice with two verdicts.  The
# preprocessor is gone: every RUN is raw, every fixture has one
# verdict, and where the two disagreed the `raw` row is the one kept.
# Task #219: stream-provided `_model` records are no longer supported —
# the in-process modeller is the only model source, and a record whose
# name carries a `_model` component is an ordinary declaration with no
# effect on any inductive block.  The 34 fixture streams that carried
# preprocessor-era model families were therefore regenerated: three
# from their `.lean` sources with `scripts/export-fixture.sh`
# (direct_nested_dep, indexed_nested_aux, nested_pin_names) and 27 —
# the nat_* slices, trust_*, indexed_one_ctor_proj,
# presieve_ofarrows_cone — by dropping the model declaration records
# from the committed slice together with the table entries only they
# kept alive, which is what a raw slice carries.  No verdict moved.  `direct_nested_dep_broken.ndjson` was DELETED: it was
# the positive-control's twin with the `Box._model` family head removed,
# and it existed to pin that a model generator's skip rule must be
# dependency-aware — there is no such generator and no such rule.  Two
# fixtures keep `_model` NAMES on purpose: `model_name_plain` (the
# negative control — a plain `def Foo._model` must accept) and
# `yolo_decline_vs_accept` (its defective theorem happens to be called
# `PProd'._model.eta`; it is checked, and rejected, as the ordinary
# declaration it is).
#
# An expectation is a single exit code.  (The direct simple-structure
# route is the priority route for every recognised block since task
# #175 W4c — no master switch, no artifact-absence gate; see
# tests/arena-expected.txt's header and `ConLeche/Kernel/Direct.lean`.)
0 indexed_vec.ndjson
0 prop_proj.ndjson
# nat_ops.ndjson: all seven structural-Nat fast-path operations
# (pred/add/sub/mul/pow/beq/ble) exercised on 2^64-scale literals; the
# certified install path must keep accepting the standard definitions.
0 nat_ops.ndjson
# prop_proj_raw.ndjson (= arena tutorial 087, only reachable from raw
# kernel declarations, not surface Lean): a *Prop* structure-like with
# data fields (PUnit.{v} at v=1) among its Prop fields.  For this
# shape lean-inductive-models emits no _model.proj_i artifacts — and
# cannot in general: the projections only exist at certain level
# instantiations (here u=0 collapsing the fields to Prop), while the
# artifacts would have to be level-polymorphic.  con-leche's
# recursor-inlining fallback is therefore the permanent mechanism for
# this shape: it builds the elimination at the use site's concrete
# levels.  (All-Prop structures DO get artifacts; a Prop structure
# with a data field cannot be written with the `structure` command,
# so no .lean source exists for this fixture.)
0 prop_proj_raw.ndjson
# sorryAx handling: an unused sorryAx axiom record is skipped (no set
# model exists for it) without even parsing its type (tolerated-axiom
# records are invisible; since the taint skip-and-continue their types
# are no longer well-formedness-checked either — `Lean.ofReduceNat`'s
# own type references the tainted `Lean.reduceNat`), a use makes the
# stream decline.  sorry_unused is hand-patched (an export never
# contains an unused axiom record).
0 sorry_unused.ndjson
2 sorry_use.ndjson
# Tolerated axioms (whitelist, user ruling) are invisible: the axiom
# record is skipped without stopping the run — an independent theorem
# after it is still accepted — and any declaration referencing the
# skipped axiom is itself skipped, with the stream declined as a whole
# at the end.  Hand-written minimal streams.
0 tolerated_axiom_unused.ndjson
2 tolerated_axiom_use.ndjson
2 custom_axiom_declined.ndjson
# Taint skip-and-continue (user directive 2026-08-24): a use of a
# tolerated axiom mid-stream is *skipped* — never checked or installed
# — and the rest of the stream is still checked; the final verdict is
# a decline (2).  The bad_later twin proves the post-skip declarations
# really are checked: the same stream with an invalid later definition
# must *reject* (1), not decline.
2 taint_skip_continue.ndjson
1 taint_skip_bad_later.ndjson
# The compiler-trust axiom family installs (task #95;
# scripts/mk_trust_fixture.py, dependency-closure slice of the full
# Init export): `Lean.trustCompiler` as an opaque realized by
# `True.intro`, `Lean.reduceNat`/`Lean.reduceBool` as pin-gated
# identity opaques, `Lean.ofReduceNat`/`Lean.ofReduceBool` as pinned
# axioms — plus a theorem *using* ofReduceNat's type vacuously.
# trust_native_use appends the kernel-visible shape of a genuinely
# native-evaluated use, `badNative : reduceBool true = true := rfl`:
# the opaque is stuck (it never delta-unfolds), so the declaration
# must reject — that hypothesis only comes from untrusted native
# evaluation (the honest limit of the trustCompiler install).
0 trust_family.ndjson
1 trust_native_use.ndjson
# trust_reduce_perturbed: reduceNat's witness value replaced by a
# type-correct non-identity (`fun n => Nat.zero`) — the build-time pin
# gate must positively decline the drifted spelling (never install:
# the ofReduce* axioms' models rely on the identity certificate).
2 trust_reduce_perturbed.ndjson
# Basis PUnit 0-field stuck-major rescue (task #59): `Unit.sizeOf`
# needs `PUnit.rec _ 1 u ≡ 1` at a neutral `u` — the official kernel's
# structure rescue covers 0-field structures; dependency-closure slice
# of the Init export through `Unit.sizeOf`.
0 unit_sizeof_rescue.ndjson
# Frontend zeta-expands rec-rule right-hand sides (task #60): the
# export carries a `let` in `Std.Packages.PreorderOfLEArgs`'s
# recursor rule; install annotates the rhs and must not see `letE`.
# Init-export slice through the block (committed gzipped).
0 let_rec_rhs.ndjson

0 opaque_decl.ndjson
# nat_ops_nonstandard.ndjson: hand-patched from a plain export —
# Nat.add's value replaced by (fun x y => x).  The structural-Nat
# fast-path certification must positively decline the nonstandard
# definition at install (never accept: the literal fast path would
# then compute values the stored definition does not).
2 nat_ops_nonstandard.ndjson
# Structural-Nat fast-path coverage (see tests/e2e/src/nat_ops*.lean).
# nat_ops_edge: zero operands, truncated sub, pred 0, 0^0, nested
# chains, succ-application and Nat.zero-constant argument forms.
0 nat_ops_edge.ndjson
# nat_*_wrong: hand-patched from the bad_base exports — ONE stated
# result literal (resp. the theorem-side Bool constant) is perturbed,
# so acceptance would require e.g. add 11 12 ≡ 24: a wrong fast-path
# value would ACCEPT, a missing fast path would exit 3 — only the
# correct certified computation rejects (1).  (The base exports were
# preprocessed and the patch had to come after that step, or the
# preprocessor's own kernel check crashed the run; task #219 dropped
# the model records the tool had added, leaving the raw slice.)
1 nat_add_wrong.ndjson
1 nat_sub_wrong.ndjson
1 nat_mul_wrong.ndjson
1 nat_pow_wrong.ndjson
1 nat_pred_wrong.ndjson
1 nat_beq_wrong.ndjson
1 nat_ble_wrong.ndjson
# A level-polymorphic definition under a fast-path name is positively
# declined at install (the certification pins level-monomorphy).
2 nat_add_levelpoly.ndjson

# nat_div_declined predates the pinned-declaration fast path (#47): a
# valid Init slice with a literal `Nat.div` use, originally declining
# via the natOpWfNames reduction safety net, then via "pin ground
# constants absent" (the slice stops short of the old cert-proof
# auxiliaries).  With the self-contained certificates (task #113) the
# slice carries everything the install needs, so the operation
# certifies and the literal reduces on the fast path: accepted.  The
# install-decline path stays covered by the nat_*_perturbed fixtures.
0 nat_div_declined.ndjson
# nat_divmod_ok.ndjson: the init-prelude prefix through Nat.div; the
# stored div/mod match the vendored toolchain pins, the ble-guarded
# characterization certificates check, and rfl-proofs of
# `20 / 3 = 6` / `20 % 3 = 2` exercise the literal fast path.
0 nat_divmod_ok.ndjson
# nat_mod_perturbed.ndjson: same prefix with Nat.mod's body replaced
# by Nat.modCore's (extensionally equal, definitionally distinct) —
# the pin gate must positively decline the drifted spelling.
2 nat_mod_perturbed.ndjson

# str_lit.ndjson (tests/e2e/src/str_lit.lean): a definition with a
# string-literal value and an Eq.refl theorem `strA ≡ "ab"` — the
# annotate/infer literal rule (type String, guarded by strLitSupported)
# and the literal-literal defeq fast path, over the stream-installed
# String/Char/List support declarations.
0 str_lit.ndjson
# str_lit_declined.ndjson: hand-patched (opaque_decl + a def whose value
# is the literal "a" before any String support declaration exists) —
# a string literal without the pinned support constants is a positively
# detected unsupported feature: decline, never reject or crash.
2 str_lit_declined.ndjson

# nested_rec.ndjson: nested inductive with a parameter (task #48) —
# `Tree.rec_1`'s List.nil/List.cons rules are `.nested`-certified iota
# rules with an *open* stored parameter instantiation (`Tree α`);
# `Tree.size`'s below/brecOn machinery only typechecks when they fire,
# and `size_example`'s rfl fires them on concrete majors.
0 nested_rec.ndjson

# nested_pin_names.ndjson (tests/e2e/src/nested_pin_names.lean; RAW
# since task #219, regenerated with scripts/export-fixture.sh):
# nested-auxiliary rules whose stored pins contain a *binder* (the
# dependent nested occurrence `DMap α (fun _ => PTree α)`) — the exact
# shape that declined Mathlib at `Lean.PrefixTreeNode.rec_3`;
# `total_example`'s rfl fires the rules on concrete majors.  The
# fixture used to be the preprocessor's output perturbed by
# scripts/mk_nested_pin_fixture.py, so that the iota theorems' majors
# spelled the pin lambda under a DIFFERENT BINDER NAME than the
# recursor type.  That perturbation is not expressible any more, twice
# over: the models come from the in-process modeller (task #219), which
# writes both sides itself, and since task #205 `Expr` carries no
# binder name at all, so there is nothing left to drift.
0 nested_pin_names.ndjson

# indexed_nested_aux.ndjson (tests/e2e/src/indexed_nested_aux.lean):
# an *indexed* nested-auxiliary recursor — `TV` nests through the
# indexed family `Vec`, so `TV.rec_1` carries an index premise
# between the prefix and the major (`majorIdx = rulePrefix + 1`).
# Task #105: the `.nested` certification covers index premises — the
# shape split reads the constructor-parameter instantiations out of
# the major domain (lowered into the prefix context; the lift-back
# roundtrip certifies no index variable occurs) and requires the
# domain's trailing arguments to be the index variables in order —
# so `TV.size`'s below/brecOn machinery fires and `size_example`'s
# rfl reduces.  Accepted end to end (previously a positive decline,
# exit 2, at `TV.brecOn.go`).
0 indexed_nested_aux.ndjson

# str_proj.ndjson (tests/e2e/src/str_proj.lean): a theorem forcing
# `.proj String 0 ""` (the `String.utf8ByteSize_empty` shape:
# `utf8ByteSize` is `ByteArray.size ∘ toByteArray` and `toByteArray`'s
# value is `fun s => .proj String 0 s`).  Covers the references' proj
# expansion site (task #52).  NOTE: on annotated input the literal is
# in fact forced through the *rec-major* expansion (annotate rewrites
# template-entry projections into the installed projection recursor,
# so `.proj` nodes with a String scrutinee never survive annotation);
# the whnf proj-site expansion itself is reference-mirroring but
# unreachable for annotated input — see DESIGN.md "String literals".
0 str_proj.ndjson
# str_proj_declined.ndjson: the same stream with Char.ofNat perturbed
# to carry a (unused, unreferenced — the stream stays valid) level
# parameter, so `strLitSupported` fails.  The literal in the theorem
# statement is then a positively detected unsupported feature at
# annotate: decline (2) — the guard front-door fires before any
# reduction, so the stuck-proj behavior never becomes a verdict.
2 str_proj_declined.ndjson

# Pin-certified GMP Nat operations (task #54): per operation, a
# dependency-closure slice of the full-Init export through the
# operation's own declaration (scripts/mk_natop_fixture.py; committed
# gzipped) plus an Eq.refl theorem on literals exercising the certified
# literal fast path; and the same slice with the operation's body
# replaced by a type-correct but different definition, which the pin
# gate must positively decline.
0 nat_gcd_ok.ndjson
2 nat_gcd_perturbed.ndjson
0 nat_land_ok.ndjson
2 nat_land_perturbed.ndjson
0 nat_lor_ok.ndjson
2 nat_lor_perturbed.ndjson
0 nat_xor_ok.ndjson
2 nat_xor_perturbed.ndjson
0 nat_shiftleft_ok.ndjson
2 nat_shiftleft_perturbed.ndjson
0 nat_shiftright_ok.ndjson
2 nat_shiftright_perturbed.ndjson
# nat_log2_ok: since the audit's S1 (`Nat.log2` left the literal op
# set — official's reduce_nat folds nothing unary but Nat.succ, and
# the install-time pin went with the fast path) this fixture pins the
# ORDINARY route: a stream declaring Nat.log2 and applying it to a
# literal must still accept, by unfolding, with no acceleration and no
# decline.  Its perturbed twin was deleted with the pin: a
# differently-defined Nat.log2 is now accepted, as official accepts it.
0 nat_log2_ok.ndjson
# Self-contained certificate blobs (task #113): *pure-cone* slices —
# the operation's dependency closure plus only the guard-required
# ground operations (scripts/natop_cone_roots.json), with the cert
# proofs' historical auxiliary constants (funext, Eq.subst,
# of_decide_eq_true, …) positively absent from the stream — must
# accept: the generated proofs may only reference the op's own cone
# (which every stream declaring the op provides).  (nat_log2_cone was
# the second slice; it went with the Nat.log2 pin at the audit's S1.)
0 nat_land_cone.ndjson
# subject_reduction_redex (arena good/undecidability/subject-reduction-redex):
# `x2 x4` needs `f 1 (proof_2 x0) ≡ f 0 (proof_1 x0)` where the proofs
# are *theorems* whose values must delta-unfold for the Acc iota step
# to fire.  Theorems are OPAQUE to reduction (anticipating
# lean4#14896, which makes the official kernel stop unfolding them
# too), so this is a REJECT: the one known input whose typing needs a
# theorem to unfold — an accept-subset of the reference kernels until
# that PR lands, and the fixture that pins the opacity.
1 subject_reduction_redex.ndjson
# The And-only η rescue's probes (task #258; sources beside them, the
# use-site theorems added through the task #208 probe kit because the
# elaborator's defeq never unfolds a theorem): `And.rec` into `Nat` on a
# THEOREM-proved major accepts — the stuck proof is rescued through
# `And.intro a b h.1 h.2`, certified by proof irrelevance (an
# accept-superset of official, which never η-rescues a proposition,
# working around the absence of lean4#14925) — while the `Iff.rec` and
# `Acc.rec` twins REJECT (no rescue exists: the ruling is And-only), and
# the def-proved control accepts in both regimes.
0 and_rec_opaque.ndjson
1 iff_rec_opaque.ndjson
1 acc_rec_opaque.ndjson
0 and_rec_def.ndjson
# dag_tower (task #65/#78): a 2^28-node doubling tower (add e e at 27
# levels) over the beta-ladder prelude, DAG-shared through export-table
# indices.  Since parse-time interning (task #78) the ordinary
# definition pipeline is DAG-preserving end to end, so the record now
# ACCEPTS (formerly dag_tower_declined under the tree-size budget; cf.
# arena good/perf/app-lam at 2^4000, also accepted).
0 dag_tower.ndjson
# dag_tower_unfold (task #78): dag_tower plus a second definition with
# the same DAG-tower value and a theorem `dagTower = dagTower2 := rfl`
# whose check delta-unfolds both stored DAG-shaped values — exercising
# the interned-environment path (`IState.ienv`): the stored values are
# consumed as arena indices; re-interning the read-back tree would be
# 2^28 nodes.
0 dag_tower_unfold.ndjson
# The direct simple-structure install (task #82).  A *raw* lean4export
# fixture: the stream carries no `_model` declaration at all — the artifact-free route
# `structParts?` recognises, installed and modelled from the reference
# checks alone.  Accepted from task #82 until task #148 T0b (which
# switched the route off: both lines declined, 2, the modeled fallback
# having nothing to install from), and accepted again since task #175
# W4c made the direct route the priority route with a verified P
# install: the live witness of what the route buys.
# the model-family guard is keyed on the environment invariant's
# linkage clauses and nothing wider: a plain `def Foo` followed by a
# plain `def Foo._model` is an ordinary stream and stays accepted
# (`_model` names are not special).  Rejected (exit 1) by a blanket
# suffix reservation - which is exactly what this pins.
0 model_name_plain.ndjson
0 direct_struct_raw.ndjson
# direct_nested_dep.ndjson: a cross-block dependency — the sum `Dep`'s
# constructor mentions the structure `Box` — both on the fixpoint
# route.  RAW since task #219 (regenerated with
# scripts/export-fixture.sh): the fixture used to carry the
# preprocessor's `Box._model`/`Dep._model` families, and its twin
# `direct_nested_dep_broken.ndjson` (the same export with the
# `Box._model` family HEAD deleted, leaving a dangling artifact) was
# deleted with them — it pinned that a model generator's skip rule
# must be dependency-aware, and there is no such generator.
0 direct_nested_dep.ndjson
# malformed_midstream.ndjson: a truncated JSON record after valid
# lines — malformed input is a hard error (exit 3) with the line
# number, identically under the streaming frontend (task #57).
3 malformed_midstream.ndjson
# reducenat_guard.ndjson (task #94): defeq-side Nat-literal folding is
# guarded on both sides being fvar-free (official kernel
# lazy_delta_reduction; lean4lean TypeChecker.lean:782).  Unguarded,
# this open pair whnfs the open argument `x + 2147483647`,
# delta-unfolds Nat.add, and grinds the Nat.brecOn tower down the 2^31
# literal to fuel exhaustion (exit 3); guarded it reduces cheaply.
# Minimal spelling of the Int32 instUpwardEnumerable_eq detonation in
# the full Init export (kept as _tmp/init-exports/
# repro-int32-upward-enum-pre.ndjson, see DESIGN).
0 reducenat_guard.ndjson
# k_major_raw.ndjson (the Mathlib decide-over-Rat frontier, 2026-09-06):
# the K rescue converts the major of a K-flagged recursor BEFORE its
# whnf, as the official kernel's inductive_reduce_rec (to_ctor_when_K on
# the raw major, reading only its type) and lean4lean do.  With the
# whnf first, the major -- a theorem applied to the literal 604800 --
# delta-unfolds and its Nat.rec proof grinds the literal unarily to
# fuel exhaustion (exit 3, both modes); in the official order Eq.refl is
# fabricated from the type (one Nat.beq literal fold) and the proof is
# never opened.  Minimal spelling of instDecidableEqRat's
# `Eq.ndrec … (Int.decEq._proof_1 … (Nat.eq_of_beq_eq_true …))` at
# Std.Time.Week.Offset.ofMilliseconds._proof_1 (record 126,329).
0 k_major_raw.ndjson

# std_axioms is a raw export whose `Iff` block has no `_model` family;
# the block installs directly since task #175 W4c (the priority gate),
# whether or not artifacts exist.  (Until task #207 this row had a
# `pre` twin, asserting that the preprocessor spawn was skipped.)
0 std_axioms.ndjson
# The direct SUM install (task #175 sum-types, 2026-09-06): non-recursive,
# non-indexed inductives with any number of constructors other than one
# — the tagged disjoint union of one tuple tower per constructor.  The
# fixtures are raw exports: not one `_model` declaration in the
# streams, every block installed by the sum route.
#   direct_sum_enum:   a three-constructor enumeration (`Color`), iota
#                      on every constructor through `Color.rec` and
#                      `Color.casesOn`; the exported `Bool`/`OfNat`
#                      blocks go direct as well.
#   direct_sum_option: `Opt` (a dependent field), the universe-
#                      polymorphic `Sum'`, `Dec p` over a `Prop`
#                      parameter (its `isFalse` field mentions `False`,
#                      a zero-constructor `Prop` inductive, direct too).
#   direct_sum_or:     `Or'` (two-constructor `Prop`: the SMALL
#                      eliminator, both rules through `Or'.elim`), the
#                      zero-constructor `Absurd : Prop` (LARGE eliminator,
#                      admissible with no constructor) and `Nil : Type`.
#   direct_sum_or_large_bad: `Or'.rec`'s motive patched to `Sort u` — a
#                      large eliminator on a two-constructor inductive
#                      whose sort may be `Prop`, which the official
#                      kernel's `elim_only_at_universe_zero` forbids;
#                      the install must REJECT (the model's iota law
#                      would be inconsistent: both constructor values
#                      are the proof point).
0 direct_sum_enum.ndjson
0 direct_sum_option.ndjson
0 direct_sum_or.ndjson
1 direct_sum_or_large_bad.ndjson
# Zero-constructor inductives at every level (task #181, 2026-09-06);
# raw exports, no `_model` declaration in the stream:
#   zero_ctor:             `PEmpty'` (universe-polymorphic, `Sort u`),
#                          `Vacant α n` (a type and a Nat parameter,
#                          `Type u`), `Bottom p` (a `Prop` with a `Prop`
#                          parameter), `Nada : Type` — each with its
#                          large eliminator used — and `absurd'` through
#                          the toolchain's `False`, which the checker
#                          installs from its own pin
#                          (`ConLeche/Kernel/Basis/False.lean`).
#   zero_ctor_false_proof: the stream plus `bogus : False := Prop` (an
#                          ill-typed value): REJECT.  The capstone
#                          `no_proof_of_False` says no accepted stream
#                          can store such a constant; this is the
#                          checker refusing one.
#   zero_ctor_bad_rec:     `Nada.rec`'s type replaced by `Type` — a
#                          recognised zero-constructor block whose
#                          recursor is not the generated one: the
#                          direct sum install REJECTS (`.invalid`; until
#                          #181 a recursor-type mismatch was a decline).
#   empty_redefined:       the one-constructor `OfNat` block renamed to
#                          `Empty`: REJECT (`reserved basis name`).
#   false_redefined:       `def False : Prop := PEmpty'.{0}` ahead of the
#                          toolchain's `False` block: REJECT.
#   false_rec_bad:         the toolchain's `False` block with `False.rec`'s
#                          type replaced by `Type` — not the pin any more:
#                          REJECT.  (User ruling, task #181: `Empty` and
#                          `False` stay pinned; a stream declaring either
#                          name any other way is invalid input.)
# All five twins are derived by scripts/mk_zero_ctor_bad.py.
0 zero_ctor.ndjson
1 zero_ctor_false_proof.ndjson
1 zero_ctor_bad_rec.ndjson
1 empty_redefined.ndjson
1 false_redefined.ndjson
1 false_rec_bad.ndjson

# The direct INDEXED install (task #175 indexed, 2026-09-06): the sum
# route widened to non-recursive, non-nested families with indices —
# any number of constructors, the carrier at an index tuple the tagged
# union of the constructor towers restricted to the index equation.
# Raw exports; every indexed block installs directly.
#   direct_idx_eq:   an `Eq`-clone (`MyEq`: Prop, one constructor, no
#                    field, one index) — LARGE elimination (`cast` into
#                    `Sort v`), iota on `refl`, and the K rescue on a
#                    neutral major (`k_use`).
#   direct_idx_vec:  `Vec' α : Nat → Type u` (three constructors at three
#                    index values) and `Par : Nat → Type` (two): iota on
#                    every constructor with constant, index-reading and
#                    small (into `Prop`) motives.
#   direct_idx_prop: `IsSucc n (n+1)` and `Both n` (one constructor, a
#                    data field occurring in the indices: LARGE
#                    elimination by official's subsingleton criterion,
#                    the field read back off the index), `Rel` (two
#                    constructors: small only), `Hidden (n+1)` (a data
#                    field not literally in the indices: small only).
#   direct_idx_prop_large_bad: `Hidden.rec`'s motive patched to `Sort u`
#                    (a large eliminator with a data field outside the
#                    indices) — REJECTED at the constructor stage
#                    (`checkStructFieldSortsI`, official's
#                    `elim_only_at_universe_zero`).
#   direct_idx_prop_idx_bad: `Rel.symm`'s second minor replaced by the
#                    first (`fun n => Rel.r n : ∀ n, Rel 0 n` where
#                    `∀ n, Rel n 0` is expected) — an index mismatch at
#                    a minor premise, REJECTED.
0 direct_idx_eq.ndjson
0 direct_idx_vec.ndjson
0 direct_idx_prop.ndjson
1 direct_idx_prop_large_bad.ndjson
1 direct_idx_prop_idx_bad.ndjson
#   direct_idx_defhead (task #193, flipped at task #195): `OfFn f :
#                    Pred α` — an indexed one-constructor family declared
#                    AT A DEFINITION (`Pred α := α → Prop`) that only
#                    unfolds to the telescope, Mathlib's
#                    `Presieve.ofArrows` shape.  #193 kept it modelled
#                    (the sum recogniser read the declared type
#                    syntactically); #195's install reads the telescope
#                    through official's whnf loop and stores the former
#                    AT THE WHNF'D TELESCOPE, so the block goes DIRECT
#                    (route `sum`, beside the control `Rel`).
0 direct_idx_defhead.ndjson
#   presieve_ofarrows_cone (task #195): the dependency cone of Mathlib's
#                    `CategoryTheory.Presieve.ofArrows` (with the
#                    String-support constants).  The block whose
#                    "missing model" decline at 7.5 % of the
#                    regenerated Mathlib stream was task #193's
#                    finding; it installs here (503 declarations, both
#                    modes).  The cone was cut from a stream the
#                    preprocessor had written; task #219 dropped its
#                    model records, so `Lean.Syntax` is the raw nested
#                    block the in-process modeller models.
0 presieve_ofarrows_cone.ndjson

# The direct FIXED-POINT route (task #188): RECURSIVE inductive types
# through the direct install — the carrier is the Knaster–Tarski least
# pre-fixed point of the constructor-tower functor, the recursor the
# fixed point of its own one-step unfolding.  Raw exports (every
# finitary recursive block installs directly); the two bad twins are
# patched off
# `direct_fix_nat.ndjson` by `scripts/mk_direct_fix_bad.py`.
#   direct_fix_nat:   `Nat'` (a `Nat` clone) and `List' α` — iota on the
#                     recursive constructors reading the inductive
#                     hypothesis (`add`, `length`, `append`, `map`), on
#                     the nullary ones, dropping the hypothesis (`pred`),
#                     into `Prop` (`small`).
#   direct_fix_tree:  `Tree α` (two recursive fields around a data field:
#                     two hypotheses per minor, `mirror`/`depth`) and the
#                     one-constructor recursive `Chain α` (NOT
#                     structure-like: official's `is_structure_like`
#                     excludes recursive blocks).
#   direct_fix_prop:  the two-constructor recursive `Prop`s `Ev` and
#                     `Chain' P` (a data and a proof field before the
#                     recursive one) — SMALL elimination only.
#   direct_fix_prop_large: `Loop : Prop` with ONE recursive constructor:
#                     the official kernel gives it the LARGE eliminator
#                     (every field is a proposition) — the recursive
#                     squash regime, taken since task #202 Stage A2.
#   direct_fix_nat_neg_bad: `Nat'.succ`'s field patched to `Nat' → Nat'` —
#                     a non-positive occurrence, REJECTED as official
#                     (`check_positivity`).
#   direct_fix_nat_ih_bad: `Nat'.rec`'s `n_ih` binder patched from
#                     `motive n` to `Nat'` — the stream's recursor is not
#                     the generated one, REJECTED at the S2 comparison.
# INDEXED recursive families (the route is indexed from the start):
#   direct_fix_vec:   `Vec' α n` with the recursive `cons` at index
#                     `Nat.succ n` (`length`, `map` with a motive reading
#                     the index, `headD`, `small`) and `Tm k n` — a
#                     parameter and an index, two recursive fields at
#                     different index expressions in one constructor.
#   direct_fix_le:    the two-index `Le' a b` (added by `addDecl`: the
#                     `inductive` command would promote the fixed first
#                     index) and `Even' n` — recursive `Prop`s with two
#                     constructors, SMALL elimination only.
#   direct_fix_acc_large: `Up n (n+1)` with ONE recursive constructor
#                     and the large eliminator (the data field is an
#                     index): taken, as `Loop` (task #202 Stage A2).
#   direct_fix_vec_idx_occ_bad, direct_fix_vec_res_occ_bad (task #210
#                     Part D): `Vec'.cons` with an occurrence of the
#                     block inside an INDEX expression — the recursive
#                     field's index `(fun β => n) (Vec' α n)`, resp. the
#                     result's `(fun β => Nat.succ n) (Vec' α n)`.  Both
#                     whnf to the original index, but official's
#                     `is_valid_ind_app` runs `has_ind_occ` on every
#                     index argument unreduced: "non valid occurrence",
#                     resp. "invalid return type" — REJECTED (1), as
#                     `recFamOk` / `recCtorKinds`' residual check do
#                     (`.negative`); before Part D a decline.
#   direct_fix_vec_ih_idx_bad: `Vec'.rec`'s `v_ih` binder patched to
#                     `motive (Nat.succ n) v` — the hypothesis at the
#                     wrong index, REJECTED (the patched recursor type
#                     is ill-typed before the S2 comparison is reached).
# REFLEXIVE constructors (task #202, Stage A1: `Prop`-valued blocks with
# the small eliminator):
#   direct_fix_refl:  `Iter f n` with `step (n) (h : ∀ k, Nat.le k n →
#                     Iter f k)` — a function-space field into the
#                     fibres at the bound variable, the inductive
#                     hypothesis pointwise (`∀ k a, motive k (h k a)`),
#                     and the two-index `Reach r x y` with a reflexive
#                     field beside a finitary one; consumers fire iota
#                     on both (`elim_step` by `rfl`).
#   direct_fix_refl_neg_bad: `Iter.step`'s field patched to
#                     `∀ k, Iter f k → Nat.le k n` — a non-positive
#                     occurrence under the binder, REJECTED (positivity).
#   direct_fix_refl_ih_bad: `Iter.rec`'s `h_ih` binder patched to
#                     `∀ k a, motive n (h k a)` — the hypothesis at the
#                     wrong index, REJECTED (ill-typed recursor type).
#   direct_fix_acc:   an `Acc` clone (task #202 Stage A2) — ONE
#                     constructor, the data field an index, the
#                     reflexive field a `Prop`-valued function; the
#                     official kernel gives it the LARGE eliminator
#                     (subsingleton elimination); consumers eliminate
#                     into `Type` (`Acc'.fix`, iota by `rfl`); the
#                     export also carries `Acc'.below`, the
#                     `Acc.below`-shaped two-index `Prop` block (one
#                     constructor, large eliminator), both on the
#                     `fix` route.
# `Type`-VALUED reflexive blocks (task #202 Stage B: the family's closed
# member by the container construction, the recursor by the recursion
# theorem over the elements; raw exports, no `_model` artifacts):
#   direct_fix_wtype: the W-type `W' α β` (`sup (a : α) (f : β a → W' α
#                     β)`, the telescope's domain depending on the data
#                     field) and the `PSet`-shaped `PSet' : Type 1`
#                     (`mk (α : Type) (f : α → PSet')`, the telescope
#                     domain a `Type` — the universe bound at the
#                     family's regime); consumers eliminate into `Type`,
#                     `Type 1` and `Prop`, iota by `rfl`.
#   direct_fix_wtype_neg_bad: `W'.sup`'s field patched to `(β a → W' α β)
#                     → W' α β` — a NEGATIVE occurrence under the binder,
#                     REJECTED (positivity).
#   direct_fix_wtype_ih_bad: `W'.rec`'s `f_ih` binder patched to `Type` —
#                     the ih of the wrong type, REJECTED (recursor
#                     comparison).
#   direct_fix_term:  the first-order-term-shaped `Term' F α` (`func (n)
#                     (f : F n) (ts : Fin n → Term' F α)`: the
#                     telescope's domain reads an earlier data field,
#                     beside an ordinary constructor), the
#                     `PFunctor.Approx.CofixA`-shaped INDEXED `CofixA' A
#                     B n` (reflexive at the predecessor index, two
#                     constructors) and the `Turing.PartrecToTM2.Λ'`-
#                     shaped `Lam'` (reflexive and finitary fields mixed
#                     over five constructors); consumers eliminate into
#                     `Type` (dependent motives, a `match` on the index
#                     inside a motive), iota by `rfl`.
0 direct_fix_nat.ndjson
0 direct_fix_tree.ndjson
0 direct_fix_prop.ndjson
0 direct_fix_prop_large.ndjson
1 direct_fix_nat_neg_bad.ndjson
1 direct_fix_nat_ih_bad.ndjson
0 direct_fix_vec.ndjson
0 direct_fix_le.ndjson
0 direct_fix_acc_large.ndjson
1 direct_fix_vec_ih_idx_bad.ndjson
1 direct_fix_vec_idx_occ_bad.ndjson
1 direct_fix_vec_res_occ_bad.ndjson
# TASK #220 — THE TYPE-AND-CONSTRUCTOR GATE, SPLIT OFF THE RECURSOR PIN.
# Six twins of `direct_fix_nat.ndjson`, patched by
# `scripts/mk_ind_stub_bad.py`, each carrying a BROKEN RECURSOR RECORD:
# the arena's finding-F1 shape, where a stub recursor used to make the
# recogniser refuse the block so that the semantic reject was never
# reached.  Official reads no recursor from the stream — `add_inductive`
# GENERATES it and the replay compares the exported record with the
# generated one structurally (`checkPostponedRecursors`) — so all six
# are REJECTS (1) for official too.
#   ind_stub_rec:     `Nat'.rec`'s record blanked (`numMotives = 0`,
#                     `numMinors = 0`, `rules = []`) beside an intact
#                     block: the reject is the pin's own, "recursor rules
#                     are not the generated ones".
#   ind_stub_pos:     the same stub, plus `Nat'.succ`'s field patched to
#                     `Nat' → Nat'` — the reject is POSITIVITY's, reached
#                     because the gate no longer waits on the recursor.
#   ind_stub_resid:   the same stub, plus `Nat'.zero : Nat' → Nat'` at
#                     `numFields = 0` — official's `is_valid_ind_app` on
#                     the constructor's result ("invalid return type").
#   ind_stub_univ:    the same stub, plus `Nat'.succ`'s field patched to
#                     `Type` — a field universe too large for a `Type 0`
#                     block.
#   ind_stub_recname: the block intact, its recursor record (and the
#                     occurrence its rules recurse through) named
#                     `Nat'.not_rec`: official generates only `Nat'.rec`
#                     and its replay rejects with "No such recursor".
#   ind_stub_reclps:  the block intact, its recursor record's level
#                     parameters `[u, u]` — neither the block's own (the
#                     small eliminator) nor a fresh one in front (the
#                     large one).
1 ind_stub_rec.ndjson
1 ind_stub_pos.ndjson
1 ind_stub_resid.ndjson
1 ind_stub_univ.ndjson
1 ind_stub_recname.ndjson
1 ind_stub_reclps.ndjson
# TASK #228 — THE DECLARED PARAMETER COUNT.  Two more twins of
# `direct_fix_nat.ndjson`, patched by `scripts/mk_ind_numparams.py`,
# which touch INDUCTIVE-RECORD METADATA and nothing else.  Official's
# replay hands `add_inductive` the `numParams` an inductive record
# declares and checks the block against it — the type formers'
# telescopes (`check_inductive_types`, "number of parameters mismatch")
# and, through the postponed comparison, every constructor record's own
# count.  Its `numIndices` it never reads: nothing compares an inductive
# record against the generated `InductiveVal`.
#   ind_numparams_bad:     `numParams = 1` declared on `Nat' : Type`,
#                          a former with no Pi binder at all — official
#                          rejects (1).  Before task #228 the count came
#                          off the constructors, which all say 0, and
#                          this stream ACCEPTED: the row is the
#                          accept-superset that task closed.
#   ind_numparams_indices: every nonzero declared `numIndices` bumped by
#                          three, `numParams` untouched — an unread
#                          field, so the stream still ACCEPTS (0).  The
#                          guard on the change's one-sidedness, over a
#                          block that carries both counts (`numParams =
#                          2` with one index).
1 ind_numparams_bad.ndjson
0 ind_numparams_indices.ndjson
0 direct_fix_refl.ndjson
1 direct_fix_refl_neg_bad.ndjson
1 direct_fix_refl_ih_bad.ndjson
0 direct_fix_acc.ndjson
0 direct_fix_wtype.ndjson
1 direct_fix_wtype_neg_bad.ndjson
1 direct_fix_wtype_ih_bad.ndjson
0 direct_fix_term.ndjson
# STRUCTURE-LIKE recursive blocks (task #210 Part A: one constructor, no
# index — the fixpoint route conses the block's PROJECTION TABLE at the
# tagged tower's offset 1 and pins the structure route's capability
# record, `nativeCaps`; raw exports, no `_model` artifacts):
#   direct_fix_struct_proj: `Chain` (a data field before the recursive
#                     one: projection iota on both fields, a nested
#                     `.t.h`, a definition through projections), `Dep α β`
#                     (two parameters, a DEPENDENT field `b : β a` whose
#                     projection's type reads the earlier field, a
#                     recursive field) and `PChain P : Prop` (a proof
#                     field and a recursive one: the squash regime, the
#                     projection typed under the `Prop` guard).  The
#                     elaborator's projection FUNCTIONS are raw `.proj`
#                     nodes; official types them (`infer_proj` ignores
#                     recursion).
#   direct_fix_struct_proj_idx_bad: `Chain.h := fun self => self.2` — the
#                     index past the fields, REJECTED (as official's
#                     `infer_proj`).
#   direct_fix_struct_proj_iota_bad: `Chain.t_mk`'s statement patched to
#                     `(Chain.mk 3 t).t = Chain.mk 3 t` under `Eq.refl`:
#                     iota reduces the left side to `t`, REJECTED.
#   direct_fix_struct_eta: structure η at the recursive `Chain` (`c =
#                     Chain.mk c.1 c.2 := Eq.refl c`, added with the kernel
#                     check skipped) — REJECTED, as official: its
#                     `is_structure_like` is `!is_rec()` too, so no
#                     `try_eta_struct` at a recursive block; con-leche's
#                     `nativeCaps` claims η only at a block without a
#                     recursive or reflexive field.  (Granting it here was
#                     tried: sound in the model, but `ind_nest_via_refl`'s
#                     tool-modelled nesting over a reflexive structure-like
#                     then spun `isDefEq` — official's `!is_rec` is
#                     load-bearing.)  Official: 1.
0 direct_fix_struct_proj.ndjson
1 direct_fix_struct_proj_idx_bad.ndjson
1 direct_fix_struct_proj_iota_bad.ndjson
1 direct_fix_struct_eta.ndjson

# Stuck-major structure eta on the pinned `PSigma'` basis (task #61).
# A *raw* export (`PSigma'` and the theorem are kernel-added by the
# fixture source, exactly as a `_model` family
# splices the tight pair, so the block matches con-leche's pin -- a
# non-matching block would be rejected as a reserved basis name).
# `Eq.refl (motive t) (minor t.1 t.2)` at a *neutral* `t` forces both
# eta sides: `motive (PSigma'.mk a b t.1 t.2) == motive t` (defeq-side
# pair eta, `pairEtaCert`) and `PSigma'.rec a b motive minor t ==
# minor t.1 t.2` (the stuck-major rescue `to_cnstr_when_structure`).
# The rescue's eta branch is *inert* for `PSigma'` (the generic
# `structEtaCertWith` excludes reserved basis names; the 0-field
# `PUnit` fallback needs `etaFields = 0`), yet the verdict matches the
# official kernel's: `PSigma'.rec` is Prop-eliminating only, so every
# term the rescue could produce is a proof and proof irrelevance
# identifies the two sides.  The fixture pins that agreement -- it must
# stay 0 whether or not the rescue is ever made to fire here.
0 psigma_rec_eta.ndjson

# The class-B yolo escape, exercised (2026-08-27 fuzz campaign,
# finding F1).  Under CON_LECHE_NO_PROOF_CERTS=1 `inferSpineT` drops the
# whole per-argument `infer` — not just the argument/domain `defeq` —
# so *any* defect living only inside an application ARGUMENT escapes
# the measurement stack.  These three hand-built streams are the
# certified stack's side of that: each must give the verdict on this
# line, while tests/yolo-expected.txt records what yolo gives instead
# (0 for all three).  They are the only fixtures in either suite with
# a yolo override; keeping them here — rather than only in a campaign
# scratch directory — is what makes the divergence a tested property.
#
# yolo_arg_escape: `theorem everything : ∀ p : Prop, p :=
#   fun p => (fun h : p => h) (Eq.refl Nat Nat.zero)`.  The redex's
# result type really is `p`, so only the argument check can see that
# `rfl : 0 = 0` is not a proof of `p`: rejected here, accepted under
# yolo, which therefore "proves" ∀ p : Prop, p.
1 yolo_arg_escape.ndjson
# yolo_arg_escape_univs: `def w9 : Nat := (fun x : Nat => Nat.zero)
#   Nat.zero.{1}` — the escaping defect is a wrong universe-argument
# count on a constant in argument position, not a type mismatch at
# all.  It is here to pin that the class is the argument *position*,
# not one particular error: dropping the per-argument `infer` also
# drops that argument's universe-arity, non-function-application,
# binder-domain-is-a-sort and projection-shape checks.
1 yolo_arg_escape_univs.ndjson
# yolo_decline_vs_accept: a purpose-built defective family whose
# `PProd'._model.eta` theorem — an ORDINARY declaration, the `_model`
# component of its name meaning nothing since task #219 — reaches an
# unsupported projection shape (`projection
# on a non-structure type`) from inside an application argument.  The
# certified stack positively DECLINES it (2); yolo never infers the
# argument, so the unsupported feature is never detected and the
# stream is accepted (0).  The escape is therefore not confined to
# reject-vs-accept — a decline can be lost the same way.
# Task #161 P5: the certified verdict moved from 2 to 1.  The annotate
# pass infers each binder's codomain sort to write its datum, so the
# per-argument type error in this purpose-built defective stream
# ("function expected", theorem PProd'._model.eta) is now found before
# annotate reaches the unsupported projection shape that used to
# decline first.  An ordering change on DEFECTIVE input only — a valid
# stream has no such error — and it makes the certified lane agree with
# the --trusted lane on this fixture (its override is gone).  The
# class-B property the fixture pins is unchanged: the defect lives in
# an application argument.
1 yolo_decline_vs_accept.ndjson
# basis_binderinfo{,_rec} (task #142, 2026-08-27 fuzz campaign finding
# F2): raw lean4export streams that are byte-for-byte a good stream
# except that one binder of a *pinned basis block* carries a different
# `BinderInfo` — `Nat.succ : {n : Nat} → Nat` in the first, a binder
# of `Nat.rec` in the second.  Kernel typing erases binder annotations,
# so the official kernel accepts both; before #142 the block missed
# every pin and fell through to the alias path, which rejected the
# reserved name `Nat` (exit 1 — a *soundness-shaped* false reject).
# The frontend now maps every parsed binder to `.default`, so the
# deviation cannot exist downstream at all.
0 basis_binderinfo.ndjson
0 basis_binderinfo_rec.ndjson
# irrel_commit (task #161 residual round 2, BOOL FALL-THROUGH vs
# OFFICIAL'S COMMIT SEMANTICS): the fixture that separates con-leche's
# fall-through from the official kernel's commit.  At the argument pair
# `F a` vs `F (Acc.intro x g)` proof irrelevance FAILS in every kernel
# (the two propositions — a stuck `Acc.rec` application and its iota
# reduct's spelling — are not defeq), and the NEXT rule, spine
# congruence, succeeds (same fvar head `F`; the arguments `a` and
# `Acc.intro x g` are proofs of the SAME proposition `Acc r x`).
# Official COMMITS at the proof-irrelevance failure
# (type_checker.cpp:1202-1203, lean4lean TypeChecker.lean:855-856) and
# REJECTS (verified: Lean v4.33.0 "(kernel) declaration type mismatch";
# lean4lean throws a kernel exception).  con-leche's `proofIrrel` returns a
# `Bool`, the `false` falls through, and the congruence accepts — with
# AND without the pending proofIrrel common-type restriction.
# Expectation 0 records the CURRENT (divergent) verdict; it flips to 1
# if the three-valued commit is ever threaded through `defeqStep`.
0 irrel_commit.ndjson
# natop_arg_order.ndjson: the divergence audit's D15 regression (DESIGN.md
# "THE DIVERGENCE AUDIT") — `Nat.add o (slow 80000) = Nat.add (id o)
# (slow 80000)` with `o` opaque: official's reduce_bin_nat_op never
# evaluates the second argument (the first is stuck); before D15 con-leche
# whnf'd both and died of fuel (exit 3)
0 natop_arg_order.ndjson
# delta_chain.ndjson: the audit's D3 witness shape (two 300-step lockstep
# delta chains compared by rfl); a plain accept, kept as the fixture the
# lazy-delta restructuring is measured on
0 delta_chain.ndjson
# binder_shared_local.ndjson: task #201 — the defeq binder arms open BOTH
# bodies with ONE local (official `is_def_eq_binding`); before the fix two
# bodies equal up to the bound variable's display name were not `==`, the
# `.proj` whnfCore clause whnf'd `g x` on an open `x`, and `Nat.mul x 2^32`
# was iota-ground unarily (exit 3, fuel).  Official accepts.
0 binder_shared_local.ndjson
# binder_name_proj.ndjson: task #203 — `==` and the packed hash are
# α-equivalence (binder names and binder infos are parsed to ONE normal
# form, `.anonymous`/`.default`).  The statement `.proj 1 (h (fun y => y)
# x) = .proj 1 (h (fun tw_1 => tw_1) x)` with the two λs distinct table
# nodes differing in the binder name and info alone, the struct a
# 40 000-step Nat.rec tower (`scripts/mk_binder_twin_fixture.py --proj`);
# before #203 the pair missed the fast path and the `.proj` clause
# whnf'd both structs in full (3.38 G instructions against 0.37 G).
# The stream must reach the checker raw — until task #207 that took
# saying, since the preprocessor's re-export hash-consed up to α and
# would have collapsed the twin first.  Official accepts.
0 binder_name_proj.ndjson
# mutual_struct_proj / nested_struct_proj (the projection-function
# rewrite, ConLeche/Frontend/ProjRec.lean, 2026-09-06): the elaborator's
# `T.f := fun self => self.i` on structure-likes the direct install
# does not serve — a MUTUAL pair of structures (dependent field, proof
# field at elimination level 0, recursive field across the block), a
# NESTED structure (`kids : List Tree`, auxiliary motive/minors) and a
# reflexive RECURSIVE one (`tail : Unit → Stream'`) — are rewritten to
# `T.rec` applications by the frontend before checking; the `rfl`
# consumers force iota through them.  Pre-rewrite verdict: 2
# ("projection on a non-structure-like type", the Mathlib frontier's
# decline at Lean.Meta.Grind.AC.DiseqCnstr.lhs).
# TASK #207: 2.  The reflexive-member class went native at #202, but
# only for a block of its own: a MUTUAL block with a reflexive member
# still goes to the in-process modeller, whose mutual rung declines it
# ("in-process model of MutualStructProj.Node: reflexive member").
# The preprocessor used to model it; there is no tool any more, so
# this is the residual class the mutual rung owes — a positive decline
# naming it.  (nested_struct_proj, the same fixture family, ACCEPTS:
# its reflexive member is not in a mutual block.)
2 mutual_struct_proj.ndjson
0 nested_struct_proj.ndjson
# inmodel_mutual.ndjson (task #200 B1, the in-process modeller): three
# index-free MUTUAL blocks — a data pair with cross recursion, a
# structure-like pair with a universe-polymorphic parameter (projection
# rewrites at the sorts the generated `proj_i.iota` artifacts name), a
# Prop pair with a small eliminator.  The in-process modeller takes
# every one of them (route `inmodel`); tests/inmodel.sh additionally
# re-checks its dump, where the models come from the stream instead.
0 inmodel_mutual.ndjson
# inmodel_mutual_idx.ndjson (task #200 B2): four INDEXED mutual blocks —
# a Prop pair over Nat, a data pair with a shifted-index constructor and
# a recursor-spelled size (`rfl` through both model recursors at the
# indices), members with different index telescopes, a universe-
# polymorphic index (the tag's universe inferred).  Same routing as
# inmodel_mutual; tests/inmodel.sh gates the in-process path.
0 inmodel_mutual_idx.ndjson
# inmodel_nested.ndjson (task #200 B3): seven NESTED blocks — through
# `List`, through the indexed `Vec`, through `Option`+`Prod` (two
# mimics), through a structure `Wrap` over `List` (a mimic depending on
# a mimic), at a dependent pin `DMap α (fun _ => PT α)`, a nested
# structure with projections, and a mutual-and-nested pair.  Same
# routing as inmodel_mutual; tests/inmodel.sh
# gates the in-process path.
0 inmodel_nested.ndjson
# inmodel_groups.ndjson (task #200 B4): CONTAINER GROUPS — `M` nesting
# through the nested `TT` (TaggedText-shaped), `N` through the mutual
# `F`/`G`, `H` through `TT (List H)` (a group whose pin is another
# mimic's carrier).  Through the default pipe the tool models them;
# tests/inmodel.sh gates the in-process path.
0 inmodel_groups.ndjson
# indexed_one_ctor.ndjson (task #175 SigmaHom, 2026-09-06): an INDEXED
# one-constructor family in Type (Mathlib's CategoryTheory.Sigma.SigmaHom
# shape: two indices built from the fields, dependent fields).  Not
# structure-like by official's `is_non_rec_structure` (one ctor AND no
# indices), yet the model family carries `IdxHom._model.proj_{0..3}`
# and `.eta` for it (its indexed-fibre tranche); the modeled install must
# ignore those artifacts (they stay ordinary definitions) instead of
# declining at the structure-shaped residual pin.  The consumer goes
# through casesOn and a `rfl` forces the indexed iota.  Pre-fix verdict:
# 2 ("projection constructor residual arity").
0 indexed_one_ctor.ndjson
# indexed_one_ctor_proj.ndjson: the stream above plus a
# hand-written `def IdxHom.badProj := fun h => h.0` — a `.proj` node on
# the indexed family.  No projection table exists for it, so the node is
# a positive decline (user ruling: indexed types get no projections;
# decline if a `.proj` occurs).  NB the official kernel ACCEPTS this
# stream: `infer_proj` (type_checker.cpp) requires one constructor and
# `args = nparams + nindices`, not `is_non_rec_structure` — a
# conservative decline on our side, recorded as a finding in DESIGN.md.
2 indexed_one_ctor_proj.ndjson
# proj_share.ndjson (tests/e2e/src/proj_share.lean; the affine frontier,
# 2026-09-06): raw `.proj Box 0 b` nodes on a structure whose parameter
# is a 27-node DAG with a 2^27-node tree (`big% 26`, lean4export
# hash-consed).  The executable `.proj` inference clause used to compute
# the field type through the spec's tree-walking `Expr.instantiateList`,
# materializing a 134 M-node copy of the parameter per projection —
# the out-of-memory at Mathlib's
# `AlgebraicGeometry.isAffine_of_isAffineOpen_basicOpen` (DESIGN.md "The
# affine frontier").  Now through the memoized `ExprC` instantiation
# (`ProjEntry.typeAtI`); official accepts in milliseconds, so do both
# modes.  Pre-fix: out-of-memory under the 16 GB cap.
0 proj_share.ndjson
# pre_reject_nonpositive.ndjson was here until task #207: the arena's
# bad/tutorial/052_indNeg, vendored to pin "a preprocessor reject is
# our reject".  There is no external verdict to pass through any more,
# so the mapping the fixture tested no longer exists; the stream itself
# is still checked, as bad/tutorial/052_indNeg in the arena suite
# (tests/arena-expected.txt), where its verdict is recorded.
# pre_decline_imax_field.ndjson (task #180, 2026-09-07; the `pre_`
# prefix is historical — the stream is a plain raw export):
# lean-inductive-models' own `prim_shape_declines` fixture.  `PadImaxIdx`
# is an indexed one-constructor owner whose single field is a function
# type, so its level has an `imax` that the declared `max`-shaped
# carrier does not absorb in normal form — no generation arm models it,
# the tool reports the decline and exits 2, and con-leche declines (2)
# rather than checking a stream it knows is missing a model.  (`PadImax`,
# the non-indexed half, is `native` — the direct simple-structure route
# takes it — so this fixture also pins that the native predicate does
# not swallow the indexed one.)
#   Task #175 indexed (2026-09-06): `PadImaxIdx` IS in the direct
#   indexed class now (a non-recursive one-constructor family with one
#   index), `conlecheNativeSum` leaves it native, the tool exits 0 and the
#   checker installs it directly — a decline-for-missing-model turned
#   accept, the widening's intended direction; the fixture now pins
#   that the indexed owner goes native and installs.
#   Task #207 (2026-09-07): 2.  With the tool gone the stream's
#   `Foreign`/`Foreign0` blocks decide it — a constructor field whose
#   type is a DEFINITION REDEX (`idf (∀ Foreign i, Sort 3) (fun _ => N)
#   x`) that only whnf's to something classifiable.  Audit finding
#   #206-A5, the same class as ind_pos_whnf_id/fn and the three arena
#   good tests recorded in tests/arena-expected.txt; the preprocessor
#   was masking it here.  `PadImaxIdx` itself still installs directly.
#   Task #210 Part D (2026-09-07): 0.  The fixpoint route normalises
#   the field domain (`normPosDom`): the redex mentions the block and
#   whnf's to `N`, which no longer mentions it — nor the recursive
#   field `x` it was applied to, so the model's later-use guard
#   (`structUsedLater`) has nothing to fire on.  Official's verdict.
0 pre_decline_imax_field.ndjson

# THE BUILT-IN PRELUDE AND THE GROUND HOIST (task #191, 2026-09-06;
# ConLeche/Frontend/Prelude.lean, ConLeche/Frontend/NatOpGround.lean).  The
# pin-certified Nat operations were sensitive to the stream's
# installation order: `Nat.shiftLeft`'s certificate statements are
# spelled over `Nat.ble`, `Nat.sub`, `Eq` and the `Bool` values, none
# of which its own closure reaches, and lean4export's DFS order can
# emit the operation first.  Now the six basis blocks and `Bool` are
# installed FIRST, unconditionally (the prelude), and a pinned
# operation's stream-certified structural ground is HOISTED ahead of it
# (a dependency-closed reorder of the parsed list).
#   natop_order:  the RAW lean4export of tests/e2e/src/natop_order.lean
#                 in the exporter's own order — Nat.shiftLeft before
#                 Bool, Nat.ble and Nat.sub.  The user's report, as a
#                 3-theorem file.  Pre-#191 verdict: 2 ("unsupported
#                 Nat.div/mod environment (Nat.shiftLeft)").
#   natop_before_eq:  the same records with Nat.ble, Nat.sub and
#                 Nat.shiftLeft (in that order, each after its ground)
#                 hoisted ahead of `Eq` (scripts/mk_reorder_fixture.py):
#                 the prelude alone fixes this one.  Pre-#191: 2.
#   natop_before_ble: Nat.shiftLeft hoisted FIRST, ahead of Nat.ble and
#                 Nat.sub: the hoist's own case.  Pre-#191: 2.
#   prelude_bool_redefined: natop_order with `Bool : Type 1`
#                 (scripts/mk_prelude_bool_bad.py) — a valid inductive
#                 the official kernel accepts, but not the toolchain's
#                 `Bool` the prelude installed first: DECLINE (exit 2,
#                 the message names `Bool`).  Contrast the pinned basis
#                 blocks (`Empty`/`False` above): a mismatching
#                 redefinition of THOSE keeps REJECTING (reserved name).
#   A stream re-declaring `Bool` identically (natop_order itself, and
#   every fixture above that exports it) accepts with the stream copy
#   dropped; tests/arena.sh's prelude section checks the record count
#   is unchanged by the drop.
0 natop_order.ndjson
0 natop_before_eq.ndjson
0 natop_before_ble.ndjson
2 prelude_bool_redefined.ndjson

# ====================================================================
# THE INDUCTIVE-AUDIT FIXTURES (task #208; audit task #206, report
# _tmp/indaudit/REPORT.md, probes _tmp/indaudit/probes/P/*.lean).
#
# Every probe the audit ran is committed here as a fixture: the six
# divergences it found, plus the "probed and clean" shapes, which are
# regression guards.  Each source under tests/e2e/src/ names, in its
# header, the audit finding, the official verdict, the con-leche verdict at
# master 700a06ca and the route/arm responsible.
#
# All 26 exports are RAW lean4export output (lean4export caccfbe /
# leanprover/lean4:v4.29.1 — the exporter every other fixture here came
# from; ind_unsafe additionally needs its `--export-unsafe` flag, see
# that source's header).  Until task #207 each fixture got TWO lines —
# the piped verdict (the preprocessor available) and the raw one — and
# the audit's cracks lived in the gap between them.  The tool is gone,
# so only the RAW column survives, and it is the one below; the
# `TODO(#206-…)` lines keep both historical verdicts because the gap is
# what the conformance work has to close.  `--trusted` agrees with
# `--verified` on all 26 (no tests/trusted-expected.txt override).
#
# A `# TODO(#206-…)` line above an expectation marks a DIVERGENCE: the
# code below it is TODAY'S behaviour, deliberately pinned so that the
# conformance work flips it consciously.  The official verdict is on the
# TODO line; unmarked lines already agree with official.
#
# --- A1 / crack C1: def-headed former at a STRUCTURE, and its
# --- projection functions.  CLOSED by task #210 Part B: the block
# --- installs through the fixpoint route, whose recogniser reads the
# --- former's sort at the placeholder (task #195) and whose projection
# --- table (Part A) serves `.proj`; official 0.
0 ind_defhead_struct.ndjson
#
# --- A2 / crack C2: def-headed former at a K-TARGET / unit-like Prop.
# --- CLOSED by task #210 Part B (the fixpoint route, as A1); official 0.
0 ind_defhead_k.ndjson
#
# --- A3 / crack C3: def-headed formers on a MUTUAL block.  Audit
# --- follow-up 3.
# TODO(#206-A3) official: 0 — piped (until #207): the preprocessor failed (exit 3)
# TODO(#206-A3) official: 0 — raw: "former MA is not a telescope ending in a sort"
2 ind_defhead_mutual.ndjson
#
# --- the clean arms of the same family (regression guards): the
# --- RECURSIVE def-headed block and a β-redex former.  Since task
# --- #210 Part B both install natively (the placeholder sort on the
# --- one route; the declines were the modeled path's).
0 ind_defhead_fix.ndjson
0 ind_former_redex.ndjson
#
# --- A4 / crack C4: mutual members whose parameter telescopes (…param…)
# --- or result sorts (…sort…) are DEFEQ but not syntactically equal.
# --- Official compares with `is_def_eq`/`is_equivalent`; since task
# --- #218 the in-process modeller does not compare them at all — it
# --- builds the auxiliary family over the FIRST member's telescope and
# --- sort and emits the public slots at each member's own type, and the
# --- fold's typing of those slots is the comparison (CLOSED: 2 → 0).
0 ind_mutual_param_defeq.ndjson
0 ind_mutual_sort_defeq.ndjson
# --- the BAD twins (scripts/mk_mutual_bad.py): the second member's
# --- parameter domain replaced by `Type 1` / its sort by `Sort (max u 1)`.
# --- Official REJECTS ("parameters of all inductive datatypes must
# --- match" / "mutually inductive types must live in the same
# --- universe"); con-leche rejects at the generated `MB2._model` /
# --- `MD._model` record's type check — by the user's ruling the
# --- modeller may be "yolo-like", invalid input is caught in the
# --- checked code, and the message names the record, not the member.
1 ind_mutual_param_bad.ndjson
1 ind_mutual_sort_bad.ndjson
# --- the universal-coverage probe (probe kit, exact spellings): three
# --- members over a DEPENDENT telescope, the second spelling a domain
# --- as a β-redex, the third through `id` with sort `max 1 1` and an
# --- index; and a Prop pair `Prop` / `Sort (max 0 0)`.  Official 0.
0 ind_mutual_three.ndjson
# --- task #227: mutual and nested blocks whose INDEX DOMAINS' sorts are
# --- not syntactically readable (`(x : α)` at `α : id Type`, an index at
# --- a constant whose declared type is that same stuck application, a
# --- domain `FamW Nat` whose HEAD's type is stuck, the universe-poly
# --- twin, and the two closed domains on nested blocks).  The tag family
# --- carries the members' index telescopes as FIELDS, so its universe
# --- must dominate their sorts; the modeller now computes a sort CEILING
# --- (`Kit.sortCeil`) instead of a syntactic reading (CLOSED: 2 → 0).
0 ind_mutual_idxsort.ndjson
# --- its BAD twin (scripts/mk_idxsort_bad.py): the index domain `IdxW`
# --- replaced by `Nat.zero`, a term that is not a type.  Official
# --- REJECTS ("type expected"); con-leche's modeller does not validate
# --- the domain — the ceiling returns a level for any term — and the
# --- fold rejects the generated tag family ("expected a sort").
1 ind_mutual_idxsort_bad.ndjson
#
# --- A5 / crack C5: a recursive occurrence hidden under a DEFINITION.
# --- Official whnf's the field before classifying it; the fixpoint
# --- route's positivity check was syntactic.  Audit follow-up 5.
# --- ind_pos_whnf_fn is the reflexive case, ind_pos_whnf_id its
# --- finitary twin.  Task #210 Part D (2026-09-07): both ACCEPT —
# --- the route normalises the field domains by official's positivity
# --- walk before classifying them (`normPosDom`); official's verdict.
0 ind_pos_whnf_fn.ndjson
0 ind_pos_whnf_id.ndjson
#
# --- A6 / crack C6: the projection FUNCTIONS of a finitary RECURSIVE
# --- structure.  A silent regression of #188 (the block went native, so
# --- its _model.proj_i.iota artifacts went away).  Audit follow-up 2 —
# --- CLOSED by task #210 Part A: the fixpoint route conses the projection
# --- table of a structure-like block (`checkNativeTable`), so
# --- `Chain.h := fun self => self.1` types and its iota fires, piped and
# --- raw alike (cf. direct_fix_struct_proj).  Official: 0.
0 ind_rec_struct_proj.ndjson
#
# --- A7 / crack C7: raw `.proj` nodes on structure-likes the direct
# --- route does not serve.  A DOCUMENTED decline (design rule W5), not a
# --- follow-up; pinned so the design stays visible.  ind_rec_struct_proj_raw
# --- puts the recursive structure first; since task #210 Part A that one
# --- is served (its `Chain.h` accepts) and the stream declines at the
# --- MUTUAL structure's raw `.proj` (def MA.n_proj), as
# --- ind_proj_mutual_nested does.
# TODO(#206-A7) official: 0 — by design (W5); declines at MA.n_proj
# TODO(#206-A7) official: 0 — by design (W5); declines at MA.n_proj
2 ind_rec_struct_proj_raw.ndjson
# TODO(#206-A7) official: 0 — by design (W5)
# TODO(#206-A7) official: 0 — by design (W5)
2 ind_proj_mutual_nested.ndjson
#
# --- A9: reflexive blocks, infinitary nesting, nesting through a
# --- reflexive container.  Piped the tool models them all.  Raw: the
# --- reflexive block (`W'`, `Type`-valued, and the `Acc` clone) is on
# --- the `fix` route since task #202 Stage B (2 -> 0); the two NESTED
# --- ones stay documented declines of the in-process modeller (task
# --- #200's residual "an occurrence under a binder — infinitary
# --- nesting": the auxiliary family's isomorphisms with a function-typed
# --- field need function extensionality), not of the fix route.
0 ind_reflexive_tool.ndjson
# TODO(#200 residual) official: 0 — raw: the in-process modeller declines a reflexive member
2 ind_nest_inf.ndjson
# TODO(#200 residual) official: 0 — raw: the in-process modeller declines a reflexive member
2 ind_nest_via_refl.ndjson
#
# --- A10 / crack C8: `unsafe inductive`.  Official skips positivity and
# --- accepts (0); con-leche supports no unsafe declaration and DECLINES
# --- positively (2) since task #217, like unsafe axioms, opaques and
# --- definitions (arena 141/142).  The parser used to throw, so the run
# --- errored (3) — audit follow-up 6, closed.
2 ind_unsafe.ndjson
#
# --- §2 "probed and clean": shapes nobody had a fixture for and both
# --- checkers accept.  Pure regression guards — zero-constructor INDEXED
# --- families, `Sort u`-valued structure/sum with SMALL eliminators
# --- (plus `.proj` and eta on the structure), a mutual block with a
# --- zero-constructor member, a mutual index whose domain's type is not
# --- a syntactic sort, the same container at two pins / doubly nested,
# --- and Prop blocks nested through Prop containers.
0 ind_empty_idx.ndjson
0 ind_sort_u.ndjson
0 ind_mutual_zero_ctor.ndjson
0 ind_idx_defhead_sort.ndjson
0 ind_nest_two_pins.ndjson
0 ind_nest_prop.ndjson
#
# --- S1 / crack C9: the `let` value/type checks.  `annotate`'s .letE
# --- clause returns the zeta-reduct and the annotated term is let-free,
# --- so `inferBody`'s own .letE arm never sees a `letE` the driver
# --- produced: until task #217 official's `infer_let` triple ran
# --- nowhere and all three were accepted (the audit's only unlicensed
# --- accept-superset, and not an inductive matter).  #217 (follow-up 1)
# --- runs the triple inside annotate, on the annotated annotation and
# --- value, before the reduct is taken — official's three rejections
# --- (1), reached at the same declaration.  let_bad_value_used is the
# --- control the reduct's own type check already caught.
1 let_bad_value.ndjson
1 let_bad_type.ndjson
1 let_bad_thm.ndjson
1 let_bad_value_used.ndjson
# The DAG-tower fixtures (task #215; scripts/mk_tower_fixtures.py) and
# the two fixtures the retired tree-size budget left behind.  Each puts
# a shared tower of depth 60 (about 2^60 nodes unshared, ~190 entries as
# a DAG) into ONE record kind; what they gate is that every walk the
# frontend and the install run stays DAG-safe, since an unmemoized one
# does not finish on such a tower.  Task #226 completed the per-kind
# matrix: the accepting kinds below, and the four DECLINING ones after
# them, whose verdict has to be reached WITHOUT walking the tower.
0 budget_model.ndjson
0 budget_block.ndjson
0 tower_thm.ndjson
# tower_struct (task #215, wired by task #210 Part B): the tower in a
# structure's constructor field type.  The block installs through the
# fixpoint route, where every walk the install asks of the field type
# is DAG-safe (`@[csimp]` memos: `Expr.mentionsConst`,
# `liftLooseBVars`, `instantiateList`, `allLevelParamsDefined`,
# `constsResolveF`; the range-field reads `looseBVarsBounded`,
# `hasFvar`) and the constant walks go through `constsResolveFC`
# (JZero P3).
0 tower_struct.ndjson
# tower_proj (task #226): the tower in the field types of a TWO-field
# structure, so the projection bodies substitute `T.field_0` into a
# telescope that carries it (`structProjBodies`).  Exposed the pure
# `Expr.abstract1` of `closeTelescope` (`Kernel/Inductives/SumInstall.lean`),
# which rebuilt every node; it reads the fvar-range field now
# (`abstract1Fast`, `@[csimp]`, `Kernel/ExprOps.lean`).
0 tower_proj.ndjson
# The four DECLINING tower kinds (task #226).  Each reaches its verdict
# through a comparison against a pinned or built-in shape, and each of
# those comparisons is a LOCKSTEP one: it descends both terms together
# and stops at the first disagreement, so the work is bounded by the
# pin, not by the stream (`Frontend/Export.lean`, `Kernel/StdAxioms.lean`).
# tower_axiom: `Quot.sound` with a tower type -> divergent quotient pin
# (`ConstantInfo.canonEq`).
2 tower_axiom.ndjson
# tower_quot: a `quot` record for `Quot` with a tower type -> divergent
# quotient pin (`ConstantVal.canonEq`).
2 tower_quot.ndjson
# tower_prelude: an inductive block named `Bool` with a tower in its
# constructor field -> differs from the built-in prelude's `Bool`
# (`DeclC.sameCanon` through `canonEqList`).
2 tower_prelude.ndjson
# tower_axiom_pin: `propext` with a tower type, over a standardly-shaped
# stored `Iff` family (the only shape that reaches the pin comparison at
# all) -> divergent standard-axiom pin (`ConstantVal.matchesPin` through
# `Expr.erasePwEq`).
2 tower_axiom_pin.ndjson
# tower_axiom_nonstd: the same tower under a NON-pinned axiom name ->
# the positive "non-standard axiom" decline, on the name alone.  No pin
# comparison is reached; what this one gates is the annotation pass,
# which does check the tower type.
2 tower_axiom_nonstd.ndjson
# tower_usedlater (task #233): a THREE-field structure whose last
# field's type is a depth-60 tower built over the FIRST field's
# variable.  `structProjGuards` asks `structUsedLater` about the
# SECOND field, so the walk descends a tower that holds a loose
# variable ABOVE the one it looks for: the `bvarB <= i` cutoff cannot
# fire and there is no `true` to short-circuit the `||` on.  Named the
# two walkers a cutoff alone does not cover -- `Expr.hasLooseBVarB`
# (`Kernel/Inductives/StructParts.lean`) and, once past it, the pure
# rebuild inside `abstract1Fast`, whose fvar-range cutoff cannot fire
# either when the tower is built OVER the fvar being abstracted.  Both
# carry a memoized twin now (`hasLooseBVarBGo`, `abstract1Go`,
# `@[csimp]`).
0 tower_usedlater.ndjson
# tower_beqpair (task #240): two structurally equal depth-60 towers
# that are not the same objects, compared by `Expr.beq`'s memoized
# descent.  `G = fun (a b c : Nat) => a` makes `g x y z` defeq to `x`,
# so a SHARED tower `S = g S S S` and an ALTERNATING pair
# `P = g P Q P` / `Q = g Q P Q` are all defeq to `Nat.zero` and all
# structurally equal; comparing `S` with `P` asks `(S,P)`, `(S,Q)`,
# `(S,P)` one level down, so a memo keyed on the LEFT node alone finds
# its entry holding `Q` at the third query and re-walks -- 3^60 -- at
# every level.  THREE arguments is what makes it bite: at two the
# entries the first walk leaves behind still serve the second, and the
# cost is only quadratic.  Keyed on the PAIR (`beqKey`) it is two
# entries per level.  Both orientations are in the fixture, since which
# side of a declaration's defeq check is the key side is the checker's
# business: `beqPairA` carries the alternating pair in the theorem's
# type, `beqPairB` in what its value's inferred type carries, and
# exactly one of the two is the exponential one.  (It is `beqPairB`
# today -- the declared type is the memo's key side.)
0 tower_beqpair.ndjson
# tower_recfield (task #246): a RECURSIVE structure whose field after
# the recursive one is a depth-60 tower over the FIRST field's
# variable.  `nativeOpenedOk`/`nativeOpenedOkF` re-check the
# recogniser's kinds on the constructor type opened at variables, and
# at a recursive field they ask that its variable occur in no later
# field's domain -- `Expr.mentionsFvar`, i.e. `Expr.fvarLeaves`, whose
# result list is tree-sized by construction.  The answer is `false`
# here, so nothing short-circuits, and there is no cutoff to put in
# front of it: the packed `fvarB` field stops at an `fvar` leaf while
# `fvarLeaves` descends into the leaf's type annotation, so the range
# cannot answer the question at all.  The fix is at the consumer:
# `mentionsFvarGo` memoizes the `Bool` question, keyed by the node
# (`q` is fixed for the walk), `@[csimp]`.
0 tower_recfield.ndjson
# tower_mutual (task #246): a two-member MUTUAL block whose first
# constructor carries a depth-60 tower over its own first field.  A
# mutual block goes to the modeled route, so the in-process modeller
# builds its `_model` family: `classifyCtor` asks
# `InModel.mentionsAny` of every ordinary field domain and
# `InModel.hintFor`/`maxHeight` walks every generated value, both plain
# `Expr` recursions with no cutoff and, on a domain that mentions no
# member, nothing to short-circuit on.  Every earlier tower fixture is
# a single non-mutual block, so the modeller was never entered.  Both
# walkers memoize; the modeller is untrusted, so neither owes a spec
# lemma.
0 tower_mutual.ndjson
# tower_nested (task #246): a NESTED block -- a container `NBox` on the
# fixpoint route, then `NTow` nesting it -- whose constructor carries
# the same depth-60 tower.  The nested rung specialises the container
# and moves every motive, pin and domain out of the rule-prefix context
# with `Expr.lowerBVars`, a plain rebuild that had neither cutoff nor
# memo; it now carries both, as `abstract1` does (`lowerBVarsGo`, keyed
# by the node and the CUTOFF, behind an `O(1)` loose-bvar field read),
# with the kernel-checked `lowerBVarsGo_spec` and `@[csimp]`.
0 tower_nested.ndjson
# TASK #271 (issues #5 and #7) — THE INDUCTIVE BLOCK'S REDUNDANT
# FIELDS.  Official's replay compares every exported CONSTRUCTOR and
# RECURSOR record with the one `add_inductive` generates, and reads a
# type record's `ctors` list to group the constructors; the rows below
# are one-field edits of the arena tutorial's own exports, each at the
# verdict official gives.
#
# ind_ctor_order: the tutorial's `Color` with the two entries of
# `inductive.ctors` SWAPPED and `types[0].ctors` unchanged.  The block's
# constructor order is `types[].ctors`, not the record array's, so this
# is the same block and must ACCEPT (issue #5; the official kernel,
# nanoda and nanobruijn accept it).
0 ind_ctor_order.ndjson
# ind_redundant_fields: the tutorial's `TwoBool`, unedited — the
# positive control the five `TwoBool` rows below are one-field edits of.
0 ind_redundant_fields.ndjson
# `cidx` disagreeing with the constructor's position in `types[].ctors`,
# `induct` naming another declaration, `types[0].ctors` listing `mk`
# twice, `Color`'s `types[0].ctors` missing `b` (whose record is still
# there), and `numFields := 3` on a two-field constructor.  All five are
# "Invalid constructor" / "duplicate constructor name" / "No such
# constructor" for official; the `numFields` row DECLINED before this
# task, at the field telescope, which is what makes the arena's
# `ctor-num-fields` test neutral there.
1 ind_ctor_cidx_bad.ndjson
1 ind_ctor_induct_bad.ndjson
1 ind_ctors_dup_bad.ndjson
1 ind_ctors_missing_bad.ndjson
1 ind_ctor_numfields_bad.ndjson
# `k := true` on `TwoBool.rec`, whose generated recursor is not K-like
# (K is a `Prop` block with one constructor taking only the
# parameters).  Official: "Invalid recursor".
1 ind_rec_k_bad.ndjson
# ind_rule_binder_bad: the tutorial's `PredWithTypeField` (arena
# 056_predWithTypeField, the accepting control) with ONE binder type
# inside the recursor rule's λ prefix retargeted — the field binder
# `(α : Type)` reads `(α : Prop)`.  The rule's prefix is compared with
# the recursor record's own type, where the binder is still `Type`.
1 ind_rule_binder_bad.ndjson
# proj_typename_bad: the tutorial's `Prod.snd` (arena 096_projRed, the
# accepting control) with the `.proj` node's `typeName` pointing at
# another inductive.  Official: "invalid projection x.1".
1 proj_typename_bad.ndjson
