# GENERATED by tests/overview-links.sh --update — do not edit by hand.
#
# Every line-anchored link of the gated documents, in document order,
# with the lines it points at.  A diff here means a citation moved or
# its text changed: re-read the paragraph that cites it, fix the link
# or the prose, then regenerate.

>>> OVERVIEW.md

== Main.lean#L714
   714  def usage : String := String.intercalate "\n" [

== Main.lean#L48
    48  def ConLeche.CheckError.exitCode : CheckError → UInt32

== ConLeche/MainTheorem.lean#L96-L99
    96  theorem model_exists (V : Type w) [SetTheory V]
    97      (pins : List NatOpPinSet) (ds : Array Declaration) (env : Env)
    98      (accepted : checkDecls .verified pins ds = .ok env) :
    99      Nonempty (Model V env) := by

== ConLeche/Denotes.lean#L134-L135
   134  inductive Denotes (cval : Name → (LevelParam → Nat) → V) (env : Env) (φ : LevelParam → Nat) :
   135      (BVarIdx → V) → Expr → V → Prop

== ConLeche/Denotes.lean#L270-L290
   270  structure Model (V : Type w) [SetTheory V] (env : Env) where
   271    /-- the set a constant denotes, per level assignment -/
   272    cval : Name → (LevelParam → Nat) → V
   273    /-- every stored constant is a member of what its type denotes, at
   274    every level assignment (and every variable environment — the type
   275    is closed) -/
   276    mem : ∀ c ∈ env.consts, ∀ (φ : LevelParam → Nat) (ρ : BVarIdx → V),
   277      ∃ T, Denotes cval env φ ρ c.toConstantVal.type T ∧ cval c.name φ ∈ˢ T
   278    /-- whatever the built-in `False` denotes is the empty set -/
   279    false_empty : ∀ (φ : LevelParam → Nat) (ρ : BVarIdx → V) (F : V),
   280      Denotes cval env φ ρ (.const falseName []) F → F = empty
   281    /-- whatever the built-in `Eq` denotes is set equality: at a type `A`
   282    of the universe the level `u` names, and two of its members `a` and
   283    `b`, `@Eq.{u} A a b` denotes the truth value of `a = b`.  The three
   284    membership premises are the graph's domains — `Eq`'s denotation is a
   285    three-fold graph over `univ (Level.eval φ u)`, then over `A`, then
   286    over `A` again, and a graph read off its domain says nothing -/
   287    eq_equality : ∀ (u : Level) (φ : LevelParam → Nat) (ρ : BVarIdx → V) (E A a b : V),
   288      Denotes cval env φ ρ (.const eqName [u]) E →
   289      A ∈ˢ univ (Level.eval φ u) → a ∈ˢ A → b ∈ˢ A →
   290      app (app (app E A) a) b = eqv a b

== ConLeche/MainTheorem.lean#L110-L117
   110  theorem no_False_declaration (V : Type w) [SetTheory V]
   111      (pins : List NatOpPinSet) (chunks : List ByteArray)
   112      (h : jsonWithTheoremFalse chunks) :
   113      ∃ e, (do
   114        let pre ← builtinPreludeE
   115        let r ← parseChunks chunks
   116        let ds := preparePrelude pre r.decls
   117        checkDecls .verified pins ds) = .error e := by

== ConLeche/Accepts.lean#L62-L72
    62  def jsonWithTheoremFalse (chunks : List ByteArray) : Prop :=
    63    ∃ (before between₁ between₂ between₃ after name : String) (i j k v : Nat),
    64      Frontend.concatBytes chunks = (s!"{before}
    65  \{\"in\":{i},\"str\":\{\"pre\":0,\"str\":\"False\"}}
    66  {between₁}
    67  \{\"ie\":{j},\"const\":\{\"name\":{i},\"us\":[]}}
    68  {between₂}
    69  \{\"in\":{k},\"str\":\{\"pre\":0,\"str\":\"{name}\"}}
    70  {between₃}
    71  \{\"thm\":\{\"all\":[{k}],\"levelParams\":[],\"name\":{k},\"type\":{j},\"value\":{v}}}
    72  {after}").toUTF8

== ConLeche/Verify/Cached/StreamThm.lean#L207-L210
   207  theorem no_False_theorem_accepted (V : Type w) [SetTheory V]
   208      (ds : Array Declaration) (cv : ConstantVal) (v : Expr)
   209      (hmem : Declaration.thmDecl cv v ∈ ds) (hty : cv.type = .const falseName []) :
   210      ∀ env, Cached.checkDecls .verified pins ds ≠ .ok env := by

== ConLeche/Verify/Frontend/Chunks.lean#L352
   352  theorem parseChunks_ok_parseBytes {inModel census : Bool} {cs : List ByteArray}

== ConLeche/Verify/Frontend/FileFalse.lean#L177
   177  theorem parseChunks_jsonWithTheoremFalse {chunks : List ByteArray}

== ConLeche/Verify/Frontend/Prepare.lean#L174
   174  theorem mem_preparePrelude {pre : PreludeIx} {ds : Array Declaration}

== ConLeche/Verify/Cached/StreamConsts.lean#L781-L786
   781  theorem checkDecls_consts (V : Type w) [SetTheory V]
   782      {ds : Array Declaration} {env : Env} (accepted : checkDecls .verified pins ds = .ok env)
   783      {pd : Declaration} (hmem : pd ∈ ds) {cv : ConstantVal} (hcv : Declaration.Declares pd cv) :
   784      ∃ c, env.find? cv.name = some c ∧
   785        c.toConstantVal.levelParams = cv.levelParams ∧
   786        AnnotOf cv.type c.toConstantVal.type := by

== ConLeche/Verify/Cached/StreamConsts.lean#L630-L637
   630  @[expose] def Declaration.Declares : Declaration → ConstantVal → Prop
   631    | .defnDecl cv _ _, cv' => cv' = cv
   632    | .thmDecl cv _, cv' => cv' = cv
   633    | .opaqueDecl cv _, cv' => cv' = cv
   634    | .axiomDecl cv, cv' => cv' = cv ∧ cv.name ≠ sorryAxName ∧ cv.name ≠ quotSoundName
   635    | .basisDecl _, _ => False
   636    | .indDecl _ _, _ => False
   637    | .quotDecl _ _, _ => False

== ConLeche/Verify/Cached/StreamConsts.lean#L127-L128
   127  @[expose] def AnnotOf (declared stored : Expr) : Prop :=
   128    stored.resetMeta = declared.zeta.resetMeta

== ConLeche/Cached/Installed.lean#L450-L455
   450  def checkDecls (mode : CheckMode) (pins : List NatOpPinSet)
   451      (ds : Array Declaration) :
   452      Except (CheckError × Nat) Env := do
   453    let (p, _) ← (ds.foldlM (annotDeclStep mode pins) (0, mkFEnv Env.empty, #[])) {}
   454    checkPendingList mode p.2.1 p.2.2.toList
   455    pure p.2.1.env

== tests/ConLecheTests/Axioms.lean#L102-L106
   102  /--
   103  info: 'ConLeche.model_exists' depends on axioms: [propext, Classical.choice, Quot.sound]
   104  -/
   105  #guard_msgs in
   106  #print axioms ConLeche.model_exists

== ConLeche/Frontend/ExportC.lean#L934
   934  def parseExportStreamD (path : System.FilePath)

== ConLeche/Frontend/Scan/Equiv.lean#L1002
  1002  @[csimp] theorem scanLineSpec_eq_scanLineFwd : @scanLineSpec = @scanLineFwd := by

== ConLeche/Frontend/ExportC.lean#L857
   857  def chunkStep (st : StateD) (carry : ByteArray) (lineNo total : Nat) (buf0 : ByteArray) :

== Main.lean#L106
   106  def installLoop (mode : ConLeche.CheckMode) (err : IO.FS.Stream)

== ConLeche/Cached/Installed.lean#L149-L152
   149  def annotStepC (pins : List NatOpPinSet) (i : Nat) (fe : FEnv)
   150      (pend : Array PendingCheck) :
   151      Declaration → CheckCM (FEnv × Array PendingCheck)
   152    | .defnDecl cv value hint =>

== ConLeche/Cached/Installed.lean#L365-L367
   365  def checkRecord {pins : List NatOpPinSet} {ds : List Declaration}
   366      (e : InstalledEnv mode pins ds) (k : Nat)
   367      (hk : k < e.pend.size) : Except (CheckError × Nat) (PLift (GroupChecked mode e k)) :=

== Main.lean#L176
   176  def checkLoop (mode : ConLeche.CheckMode) (err : IO.FS.Stream) (stride t0 : Nat)

== Main.lean#L294
   294  def checkPool (mode : ConLeche.CheckMode) (err : IO.FS.Stream) (stride t0 jobs : Nat)

== ConLeche/Cached/Installed.lean#L399-L403
   399  def collectChecks {pins : List NatOpPinSet} {ds : List Declaration}
   400      (e : InstalledEnv mode pins ds)
   401      (tab : Array (Option (RecordResult mode e))) :
   402      (j : Nat) → (∀ i, i < j → GroupChecked mode e i) →
   403        Except (CheckError × Nat) (PLift (∀ i, GroupChecked mode e i))

== Main.lean#L333-L336
   333  def checkDeclsIO (mode : ConLeche.CheckMode) (err : IO.FS.Stream) (stride total t0 tParse jobs : Nat)
   334      (noMark : Bool) (ds : Array ConLeche.Declaration) :
   335      IO (Except (ConLeche.CheckError × Nat)
   336        { env : ConLeche.Env // ConLeche.Cached.checkDecls mode ConLeche.natOpPinSets ds = .ok env }) := do

== ConLeche/Cached/Installed.lean#L534-L536
   534  theorem fullyChecked_checkDecls {pins : List NatOpPinSet} {ds : Array Declaration}
   535      (fc : FullyChecked mode pins ds.toList) :
   536      checkDecls mode pins ds = .ok fc.env := by

== ConLeche/Cached/Installed.lean#L243-L247
   243  structure InstalledEnv (pins : List NatOpPinSet) (ds : List Declaration) where
   244    fe : FEnv
   245    pend : Array PendingCheck
   246    run : ∃ (n : Nat) (s : CState),
   247      InstallRun mode pins ds (0, mkFEnv Env.empty, #[]) {} (n, fe, pend) s

== ConLeche/Cached/Installed.lean#L201-L209
   201  inductive InstallRun (pins : List NatOpPinSet) :
   202      List Declaration → (Nat × FEnv × Array PendingCheck) → CState →
   203      (Nat × FEnv × Array PendingCheck) → CState → Prop where
   204    | nil (p : Nat × FEnv × Array PendingCheck) (s : CState) :
   205        InstallRun pins [] p s p s
   206    | cons {pd : Declaration} {ds : List Declaration} {p p₁ p' : Nat × FEnv × Array PendingCheck}
   207        {s s₁ s' : CState} (h : annotDeclStep mode pins p pd s = .ok (p₁, s₁))
   208        (rest : InstallRun pins ds p₁ s₁ p' s') :
   209        InstallRun pins (pd :: ds) p s p' s'

== ConLeche/Cached/Installed.lean#L280-L284
   280  def GroupChecked {pins : List NatOpPinSet} {ds : List Declaration}
   281      (e : InstalledEnv mode pins ds) (i : Nat) : Prop :=
   282    match e.pend[i]? with
   283    | some pc => ∃ s', checkPending mode e.fe pc {} = .ok ((), s')
   284    | none => True

== ConLeche/Cached/Installed.lean#L545-L546
   545  theorem checkDecls_fullyChecked {pins : List NatOpPinSet} {ds : Array Declaration}
   546      {env : Env} (h : checkDecls mode pins ds = .ok env) :

== ConLeche/Verify/Cached/InstalledC.lean#L456-L463
   456  theorem installRun_model (hμ : μ.verifiedChecks = true) {ds : List Declaration}
   457      {p : Nat × FEnv × Array PendingCheck} {s : CState}
   458      {q : Nat × FEnv × Array PendingCheck} {s' : CState}
   459      (hrun : InstallRun μ pins ds p s q s') :
   460      p.2.1 = mkFEnv p.2.1.env → EnvModelOk V μ p.2.1.env → CSOKF s →
   461      NodupNames q.2.1.env →
   462      (∀ pc ∈ q.2.2.toList, ∃ s'', checkPending μ q.2.1 pc {} = .ok ((), s'')) →
   463      EnvModelOk V μ q.2.1.env := by

== ConLeche/Verify/Cached/KnotCongr.lean#L543-L544
   543  theorem coreKnotI_congr (hfe : fe₁.find? = fe₂.find?) :
   544      ∀ F, coreKnotI mode fe₁ F = coreKnotI mode fe₂ F := by

== ConLeche/Verify/Cached/InstalledC.lean#L484-L486
   484  theorem fullyChecked_sound (V : Type w) [SetTheory V] (hμ : μ.verifiedChecks = true)
   485      {ds : List Declaration} (fc : FullyChecked μ pins ds) :
   486      Nonempty (EnvModelM V μ fc.env) := by

== ConLeche/Verify/Cached/MainC.lean#L72-L78
    72  theorem no_proof_of_False_cached (V : Type w) [SetTheory V]
    73      {μ : CheckMode} (hμ : μ.verifiedChecks = true)
    74      {ds : Array Declaration} {env' : Env}
    75      (h : checkDecls μ pins ds = .ok env') :
    76      ∀ c ∈ env'.consts,
    77        c.toConstantVal.type = .const falseName [] → False := by
    78    obtain ⟨mp⟩ := checkDecls_sound (V := V) hμ h

== ConLeche/Verify/Cached/MainC.lean#L50-L55
    50  theorem checkDecls_sound (hμ : μ.verifiedChecks = true)
    51      {ds : Array Declaration} {env' : Env}
    52      (h : checkDecls μ pins ds = .ok env') :
    53      Nonempty (EnvModelM V μ env') := by
    54    obtain ⟨fc, rfl⟩ := checkDecls_fullyChecked μ h
    55    exact fullyChecked_sound V hμ fc

== ConLeche/Kernel/TypeChecker.lean#L28-L54
    28  def whnfCore (env : Env) (fuel depth : Nat) (e : Expr) : CheckM Expr :=
    29    (pureFns mode env fuel).whnfCore depth e
    30  
    31  /-- The full reduction loop (fueled). -/
    32  def whnf (env : Env) (fuel depth : Nat) (e : Expr) : CheckM Expr :=
    33    (pureFns mode env fuel).whnf depth e
    34  
    35  /-- Full-grade type inference (fueled): the declaration front door's
    36  entry — official's `infer_type_core(e, infer_only = false)`. -/
    37  def inferTypeCore (env : Env) (fuel depth : Nat) (e : Expr) : CheckM Expr :=
    38    (pureFns mode env fuel).infer depth e
    39  
    40  /-- Type inference at the io grade (fueled): the knot's `inferIO` slot
    41  — what every internal inference call site runs (task #170).  At a
    42  gate-off mode this **is** `inferTypeCore` (`inferTypeIO_off`,
    43  `Verify/Knot.lean`); at the gated mode it is the io lane
    44  (`inferTypeIO_on`). -/
    45  def inferTypeIO (env : Env) (fuel depth : Nat) (e : Expr) : CheckM Expr :=
    46    (pureFns mode env fuel).inferIO depth e
    47  
    48  /-- Definitional equality (fueled). -/
    49  def isDefEqCore (env : Env) (fuel depth : Nat) (a b : Expr) : CheckM Bool :=
    50    (pureFns mode env fuel).defeq depth a b
    51  
    52  /-- The annotation pass (fueled). -/
    53  def annotateCore (env : Env) (fuel depth : Nat) (e : Expr) : CheckM Expr :=
    54    (pureFns mode env fuel).annotate depth e

== ConLeche/Kernel/Core.lean#L2908-L2917
  2908        whnf := fun _ _ => throw (.internal "fuel exhausted: whnf")
  2909        infer := fun _ _ => throw (.internal "fuel exhausted: infer")
  2910        defeq := fun _ _ _ => throw (.internal "fuel exhausted: defeq")
  2911        annotate := fun _ _ => throw (.internal "fuel exhausted: annotate")
  2912        inferIO := fun _ _ => throw (.internal "fuel exhausted: infer") }
  2913    | fuel + 1 =>
  2914      wrap
  2915        { whnfCore := fun d e =>
  2916            whnfCoreBody mode (coreKnot mode env wrap fuel) env d e
  2917          whnf := fun d e => whnfBody (coreKnot mode env wrap fuel) env d e

== ConLeche/Model/Fold.lean#L308-L315
   308  theorem no_proof_of_False_pure (V : Type w) [SetTheory V]
   309      {μ : CheckMode} (hμ : μ.verifiedChecks = true) {F : Nat}
   310      {ds : List Declaration} {env' : Env}
   311      (h : checkDeclsPure μ (fueledOps μ F) pins ds = .ok env') :
   312      ∀ c ∈ env'.consts,
   313        c.toConstantVal.type = .const falseName [] → False := by
   314    obtain ⟨mp⟩ := checkDeclsPure_sound_of (V := V) hμ h
   315    exact fun c hc hty => no_constant_of_False mp c hc hty

== ConLeche/Model/Fold.lean#L165
   165  theorem declStep_preserves (hμ : μ.verifiedChecks = true) {F : Nat} {env env₂ : Env} {d : Declaration}

== ConLeche/Model/Capstone.lean#L151-L157
   151  theorem no_constant_of_False (mp : EnvModelM V μ env)
   152      (c : ConstantInfo) (hc : c ∈ env.consts)
   153      (hty : c.toConstantVal.type = .const falseName []) : False :=
   154    no_constant_of_emptyPin mp (u := 0) (by decide)
   155      (fun ψ => by simp +decide [ConLeche.Verify.pinnedStructT, ConLeche.Term.emptyT])
   156      c hc hty
   157  

== ConLeche/Model/Denotes.lean#L379-L380
   379  noncomputable def Model.ofEnvModelM {μ : ConLeche.CheckMode} {env : Env}
   380      (m : EnvModelM V μ env) : ConLeche.Model V env where

== tests/layering.sh#L1-L3
     1  #!/usr/bin/env bash
     2  # tests/layering.sh — THE LAYERING GATE (task #161, S1; re-cut at the
     3  # SetR removal, 2026-09-05).

== ConLeche/Kernel/Env.lean#L59
    59  inductive CheckMode where

== ConLeche/Kernel/Core.lean#L2065
  2065  def whnfBody (r : CoreFns m) (env : Env) : Nat → Expr → m Expr :=

== ConLeche/Kernel/Core.lean#L2788
  2788        -- Leaf scope check, as in `inferBody`: annotation is the pass raw

== ConLeche/Kernel/PropWhen.lean#L1-L40
     1  module
     2  
     3  public import ConLeche.Kernel.Name
     4  
     5  /-!
     6  # The zero-ness datum `PropWhen` — representation, API and laws
     7  (task #161; the small-list constructors, 2026-09-06; the canonical
     8  representation, task #194, 2026-09-06)
     9  
    10  The binder annotation of the validated-annotation design: the reading
    11  of a codomain sort's zero-ness predicate `Z(l) = {φ | eval φ l = 0}`.
    12  
    13  **This module is the datum's whole boundary.**  It owns the
    14  representation, the API that is the only way to build, read and
    15  compare a datum, and — following the `Std.HashMap` pattern that
    16  `CLAUDE.md` licenses for a self-contained data-structure verification
    17  — every law about the datum *alone*.  Downstream never re-proves them
    18  and never sees a constructor:
    19  
    20  * the readout `holds` and its algebra (`holds_inter`,
    21    `holds_bindZ_go`, `holds_ext`);
    22  * the comparison — which is **equality**: `=`/`==`/`DecidableEq`
    23    decide zero-ness agreement at every valuation (`eq_iff_holds`), so
    24    the checker's validation and defeq sites compare with `==` and no
    25    separate comparison exists (task #197 deleted `equiv`);
    26  * the `inter`/`bindZ` algebra the substitution laws rest on
    27    (`inter_assoc`, `bindZ_inter`, `bindZ_go_append`,
    28    `bindZ_congr_names`, `bindZ_unit`, `paramsDefined_inter_of`).
    29  
    30  **Canonical by construction (task #194).**  Every value of the type is
    31  the unique representative of its parameter *set*: the parameter list
    32  is strictly ascending in a total order on names (`Name.cmp`, defined
    33  here), the invariant is carried by the constructors themselves, and
    34  every producer (`ifAllZero`, `inter`, `bindZ`, hence
    35  `Level.zeronessOf`/`Level.substPW`) normalizes.  Consequently
    36  `ifAllZero ps = ifAllZero qs ↔ (∀ n, n ∈ ps ↔ n ∈ qs)`
    37  (`ifAllZero_eq_iff`), `=`/`==`/`DecidableEq`/`Hashable` all decide
    38  zero-ness agreement, and the level-instantiation identity law
    39  (`Level.substPW_self`, `ConLeche/Verify/PropWhen.lean`) holds
    40  unconditionally — amendment 2's counterexample (DESIGN.md, task #161

== ConLeche/Kernel/Expr.lean#L102-L104
   102  structure BinderMeta where
   103    pw : PropWhen
   104    deriving DecidableEq, Repr, Hashable

== ConLeche/Verify/Cached/AgreeFloor.lean#L1431-L1433
  1431  theorem checkDecls_skels {mode : CheckMode} {ds : Array Declaration}
  1432      {env : Env} (h : checkDecls mode pins ds = .ok env) :
  1433      envSkels env = streamSkels ds.toList := by

== ConLeche/Term/Syntax.lean#L176-L205
   176    deriving Repr, DecidableEq, Inhabited
   177  
   178  /-- Terms.  See the module docstring for what is *not* here. -/
   179  inductive Term where
   180    /-- de Bruijn index -/
   181    | bvar (i : Nat)
   182    /-- `Sort u` at a concrete level -/
   183    | sort (u : Nat)
   184    /-- a built-in constant at a concrete level instantiation -/
   185    | const (c : BConst) (us : List Nat)
   186    /-- application -/
   187    | app (f a : Term)
   188    /-- `fun (_ : ty) => body` -/
   189    | lam (ty body : Term)
   190    /-- `(_ : ty) → body` -/
   191    | pi (ty body : Term)
   192    /-- `@Eq _ lhs rhs`, **without the type**: the interpretation is
   193    `⟦eqE a b⟧ = eqv ⟦a⟧ ⟦b⟧`, so soundness never constrains the type,
   194    and a census (task #237) found no definition and no theorem in any
   195    tier reading the slot the former used to carry.  It is gone; a
   196    producer that knows the type simply drops it. -/
   197    | eqE (lhs rhs : Term)
   198    /-- First field of a pair.  Carries **only** the subject: the pair's
   199    type arguments come from the typing premise `Γ ⊢ p : PSigma' A B`,
   200    not from the term — see the module docstring.  Interpreted by
   201    `sfst`, i.e. literally `interpExpr`'s clause. -/
   202    | fst (e : Term)
   203    /-- Second field of a pair; the `fst` twin, interpreted by `ssnd`. -/
   204    | snd (e : Term)
   205    /-- the canonical (irrelevant) proof of a derivable equation -/

== ConLeche/Semantics/Syntax.lean#L72-L98
    72    | const (c : BConst) (us : List Nat)
    73    /-- application -/
    74    | app (f a : AnnotTerm)
    75    /-- `fun (_ : ty) => body`, where `body`'s **type** has sort `u` —
    76    the codomain numeral `interp` dispatches on (tier B's F4; the
    77    #152 resolution made it derivable, and `Annotates.lam` caches it) -/
    78    | lam (u : Nat) (ty body : AnnotTerm)
    79    /-- `(_ : ty) → body`, with `ty`'s sort `u` and `body`'s sort `v` -/
    80    | pi (u v : Nat) (ty body : AnnotTerm)
    81    /-- `@Eq _ lhs rhs`; no annotation, and no type slot either, as in
    82    `Term` (task #237) -/
    83    | eqE (lhs rhs : AnnotTerm)
    84    /-- first field of a pair -/
    85    | fst (e : AnnotTerm)
    86    /-- second field of a pair -/
    87    | snd (e : AnnotTerm)
    88    /-- the canonical (irrelevant) proof of a derivable equation -/
    89    | prf
    90    deriving Repr, Inhabited
    91  
    92  namespace AnnotTerm
    93  
    94  /-- Forget the annotations. -/
    95  def erase : AnnotTerm → Term
    96    | .bvar i => .bvar i
    97    | .sort u => .sort u
    98    | .const c us => .const c us

== ConLeche/Semantics/Interp.lean#L153-L164
   153    | _, .const c us => bval V c us
   154    | ρ, .app f a => SetTheory.app (interp ρ f) (interp ρ a)
   155    | ρ, .lam v A b => lamR v (interp ρ A) fun x => interp (cons x ρ) b
   156    | ρ, .pi _ v A B => piR v (interp ρ A) fun x => interp (cons x ρ) B
   157    | ρ, .eqE a b => eqv (interp ρ a) (interp ρ b)
   158    | ρ, .fst e => sfst (interp ρ e)
   159    | ρ, .snd e => ssnd (interp ρ e)
   160    | _, .prf => pt
   161  
   162  @[simp] theorem interp_bvar (ρ : Nat → V) (i : Nat) :
   163      interp V ρ (.bvar i) = ρ i := rfl
   164  @[simp] theorem interp_sort (ρ : Nat → V) (u : Nat) :

== ConLeche/Semantics/WellDenoted.lean#L81-L111
    81  def WellDenoted : (Nat → V) → AnnotTerm → Prop
    82    | ρ, .pi _u _v A B =>
    83      WellDenoted ρ A ∧
    84      ∀ x, x ∈ˢ interp V ρ A → WellDenoted (cons x ρ) B
    85    | ρ, .lam v A b =>
    86      WellDenoted ρ A ∧
    87      (∀ x, x ∈ˢ interp V ρ A → WellDenoted (cons x ρ) b) ∧
    88      ∃ B : V → V,
    89        (∀ x, x ∈ˢ interp V ρ A → interp V (cons x ρ) b ∈ˢ B x) ∧
    90        (v = 0 → ∀ x, x ∈ˢ interp V ρ A → B x ∈ˢ (univZero : V))
    91    | ρ, .app f a =>
    92      WellDenoted ρ f ∧ WellDenoted ρ a ∧
    93      ∃ (v : Nat) (A : V) (B : V → V),
    94        interp V ρ f ∈ˢ piR v A B ∧ interp V ρ a ∈ˢ A ∧
    95        (v = 0 → ∀ x, x ∈ˢ A → B x ∈ˢ (univZero : V))
    96    | ρ, .fst e =>
    97      WellDenoted ρ e ∧
    98      ∃ u v A Bf, interp V ρ e ∈ˢ sigmaSet (Nat.max u v) A Bf ∧
    99        A ∈ˢ univ u ∧ ∀ x, x ∈ˢ A → Bf x ∈ˢ univ v
   100    | ρ, .snd e =>
   101      WellDenoted ρ e ∧
   102      ∃ u v A Bf, interp V ρ e ∈ˢ sigmaSet (Nat.max u v) A Bf ∧
   103        A ∈ˢ univ u ∧ ∀ x, x ∈ˢ A → Bf x ∈ˢ univ v
   104    | ρ, .eqE a b => WellDenoted ρ a ∧ WellDenoted ρ b
   105    | _, .bvar _ => True
   106    | _, .sort _ => True
   107    | _, .const _ _ => True
   108    | _, .prf => True
   109  
   110  /-! ### Clause equations -/
   111  

== ConLeche/Semantics/WellDenoted.lean#L303-L361
   303    have haA : interp V ρ a ∈ˢ interp V ρ A := by
   304      rw [hAA]
   305      exact hmem
   306    refine ⟨?_, ?_⟩
   307    · rw [interp_app, interp_lam, app_lamR_pos hv haA, interp_inst0]
   308    · exact (WellDenoted_inst0 V ha).mpr (hbody _ haA)
   309  
   310  
   311  /-- The graded β step at kind `0` — the residue side: with the
   312  argument membership supplied (the retained `Prop`-codomain runtime
   313  check's fact), the equality holds because both sides are the canonical
   314  proof, and the transport is the hereditary component. -/
   315  theorem WellDenoted_beta_zero {A b a : AnnotTerm} {ρ : Nat → V}
   316      (h : WellDenoted V ρ (.app (.lam 0 A b) a))
   317      (hmem : interp V ρ a ∈ˢ interp V ρ A) :
   318      interp V ρ (.app (.lam 0 A b) a) = interp V ρ (b.inst a) ∧
   319      WellDenoted V ρ (b.inst a) := by
   320    rw [WellDenoted_app] at h
   321    obtain ⟨hlam, ha, -⟩ := h
   322    rw [WellDenoted_lam] at hlam
   323    obtain ⟨-, hbody, B, hfib, hz⟩ := hlam
   324    refine ⟨?_, (WellDenoted_inst0 V ha).mpr (hbody _ hmem)⟩
   325    rw [interp_app, interp_lam, lamR_zero, app_pt, interp_inst0]
   326    exact (eq_pt_of_mem_univZero (hz rfl _ hmem) (hfib _ hmem)).symm
   327  
   328  /-! ## The app slot's establishment
   329  
   330  The clause's kind-`0` fibre component (added at the consumer seal —
   331  see the module docstring) is not a wish: it is exactly what an
   332  *annotated* `Π` hands over at the application site.  Stated at the
   333  value level, so the supplier is a theorem before the clause that
   334  consumes it is relied on. -/
   335  
   336  /-- **The app slot, established from the function type's own
   337  annotation.**  Given the function in an annotated `Π`'s
   338  interpretation, the argument in its domain, and the `Π`'s *codomain
   339  sort fact at kind `0`*, the slot follows — new component included.
   340  
   341  The codomain premise's supplier is `HasSort.mem_univ`
   342  (`ConLeche/SetR/Annot/Kinding.lean`) at the `Π`'s own numeral `v`,
   343  which is the same route `Annotates.lam`'s cached `HasSortC` takes for
   344  the λ clause's identically-shaped component.  So the two binder
   345  clauses are symmetric, which is what the amendment restores. -/
   346  theorem appSlot_of_pi {u v : Nat} {ρ : Nat → V} {f a Aa Ba : AnnotTerm}
   347      (hf : interp V ρ f ∈ˢ interp V ρ (.pi u v Aa Ba))
   348      (ha : interp V ρ a ∈ˢ interp V ρ Aa)
   349      (hcod : v = 0 → ∀ x, x ∈ˢ interp V ρ Aa →
   350        interp V (cons x ρ) Ba ∈ˢ (univZero : V)) :
   351      ∃ (v' : Nat) (A : V) (B : V → V),
   352        interp V ρ f ∈ˢ piR v' A B ∧ interp V ρ a ∈ˢ A ∧
   353        (v' = 0 → ∀ x, x ∈ˢ A → B x ∈ˢ (univZero : V)) := by
   354    rw [interp_pi] at hf
   355    exact ⟨v, interp V ρ Aa, fun x => interp V (cons x ρ) Ba,
   356      hf, ha, hcod⟩
   357  
   358  /-- The full app-clause establishment: the hereditary halves plus the
   359  slot.  This is the shape `Claims2`'s `app` case will discharge. -/
   360  theorem WellDenoted_app_of {u v : Nat} {ρ : Nat → V} {f a Aa Ba : AnnotTerm}
   361      (hokf : WellDenoted V ρ f) (hoka : WellDenoted V ρ a)

== ConLeche/Model/Annot/EnvModel.lean#L64-L100
    64  structure EnvModel (env : Env) where
    65    /-- the shared, model-free environment well-formedness -/
    66    wf : EnvWF env
    67    /-- the annotated valuation -/
    68    acval : Name → (Name → Nat) → AnnotTerm
    69    /-- every erased leaf is closed (`EnvS.cval_closed`, at `cvalE`);
    70    consumers read the `cvalE`-spelled `cval_closed` below -/
    71    cval_closedL : ∀ (n : Name) (ψ : Name → Nat),
    72      Term.Closed ((acval n ψ).erase)
    73    /-- the reserved basis constants are the pinned declarations, valued
    74    by their direct pins (V-free, shared with the TT lane); consumers
    75    read the `cvalE`-spelled `basis_pinned` below -/
    76    basis_pinnedL : BasisPinnedTT env (fun n ψ => (acval n ψ).erase)
    77    /-- every stored native projection-table entry is a pinned pair
    78    entry with its block stored (V-free, shared) -/
    79    proj_ok : ProjOkT env
    80    /-- every stored recursor rule's constructor is stored (V-free,
    81    shared) -/
    82    rec_ctors : RecCtorsStored env
    83    /-- every leaf is closed, as a lifting equation -/
    84    acval_closed : ∀ (n : Name) (ψ : Name → Nat) (k : Nat),
    85      (acval n ψ).liftN 1 k = acval n ψ
    86    /-- a leaf reads only its own level parameters -/
    87    acval_params : ∀ (n : Name) (ci : ConstantInfo),
    88      env.find? n = some ci →
    89      ∀ ψ₁ ψ₂ : Name → Nat,
    90        (∀ p ∈ ci.toConstantVal.levelParams, ψ₁ p = ψ₂ p) →
    91        acval n ψ₁ = acval n ψ₂
    92    /-- every leaf is truthful -/
    93    acval_wellDenoted : ∀ (n : Name) (ψ : Name → Nat) (ρ : Nat → V),
    94      WellDenoted V ρ (acval n ψ)
    95  
    96  variable {V}
    97  
    98  /-- **The collapsed valuation, recovered** — the re-supply the census
    99  measured at 135 sites (§1.2).  `AnnotTerm.erase` is a total syntactic
   100  function the carrier already owns, so no model content is

== ConLeche/Model/Denotes.lean#L221-L227
   221  theorem Denotes_of_denoteMeta {acval : Name → (Name → Nat) → AnnotTerm}
   222      (hcl : ∀ n ψ, Term.Closed (acval n ψ).erase) {env : Env} {φ : Name → Nat} :
   223      ∀ (d : Nat) (e : Expr) {ta : AnnotTerm},
   224        denoteMeta acval env φ d e = some ta →
   225        Expr.fvarsBelow d e → e.looseBVarsBounded 0 = true →
   226        ∀ ρ : Nat → V, WellDenotedV V ρ ta →
   227          Denotes (cvalOf (V := V) acval) env φ ρ (closeN d e) (interp V ρ ta) := by

== ConLeche/Model/Claims.lean#L92-L160
    92  @[expose] def WhnfCoreClaim (μ : CheckMode) {env : Env} (m : EnvModel V env)
    93      (φ : Name → Nat) (fuel : Nat) : Prop :=
    94    ∀ {d : Nat} {e e' : Expr} {Δa : List AnnotTerm},
    95      whnfCore μ env fuel d e = .ok e' →
    96      Expr.WScoped d e → e.looseBVarsBounded 0 = true →
    97      Expr.LeavesBounded e →
    98      ∀ {ea ea' : AnnotTerm},
    99        CtxOk m φ d Δa e →
   100        denoteMeta m.acval env φ d e = some ea →
   101        denoteMeta m.acval env φ d e' = some ea' →
   102        (∀ ρ : Nat → V, Sat V Δa ρ → WellDenotedV V ρ ea) →
   103        (∀ ρ : Nat → V, Sat V Δa ρ → WellDenotedV V ρ ea') ∧
   104          ∀ ρ : Nat → V, Sat V Δa ρ →
   105            interp V ρ ea = interp V ρ ea'
   106  
   107  /-- The reduction loop, dual success, P currency. -/
   108  @[expose] def WhnfClaim (μ : CheckMode) {env : Env} (m : EnvModel V env)
   109      (φ : Name → Nat) (fuel : Nat) : Prop :=
   110    ∀ {d : Nat} {e e' : Expr} {Δa : List AnnotTerm},
   111      whnf μ env fuel d e = .ok e' →
   112      Expr.WScoped d e → e.looseBVarsBounded 0 = true →
   113      Expr.LeavesBounded e →
   114      ∀ {ea ea' : AnnotTerm},
   115        CtxOk m φ d Δa e →
   116        denoteMeta m.acval env φ d e = some ea →
   117        denoteMeta m.acval env φ d e' = some ea' →
   118        (∀ ρ : Nat → V, Sat V Δa ρ → WellDenotedV V ρ ea) →
   119        (∀ ρ : Nat → V, Sat V Δa ρ → WellDenotedV V ρ ea') ∧
   120          ∀ ρ : Nat → V, Sat V Δa ρ →
   121            interp V ρ ea = interp V ρ ea'
   122  
   123  /-- Definitional equality, P currency. -/
   124  @[expose] def DefEqClaim (μ : CheckMode) {env : Env} (m : EnvModel V env)
   125      (φ : Name → Nat) (fuel : Nat) : Prop :=
   126    ∀ {d : Nat} {a b : Expr} {Δa : List AnnotTerm},
   127      ConLeche.isDefEqCore μ env fuel d a b = .ok true →
   128      Expr.WScoped d a → a.looseBVarsBounded 0 = true →
   129      Expr.LeavesBounded a →
   130      Expr.WScoped d b → b.looseBVarsBounded 0 = true →
   131      Expr.LeavesBounded b →
   132      ∀ {aa ba : AnnotTerm},
   133        CtxOk m φ d Δa a →
   134        CtxOk m φ d Δa b →
   135        denoteMeta m.acval env φ d a = some aa →
   136        denoteMeta m.acval env φ d b = some ba →
   137        (∀ ρ : Nat → V, Sat V Δa ρ → WellDenotedV V ρ aa) →
   138        (∀ ρ : Nat → V, Sat V Δa ρ → WellDenotedV V ρ ba) →
   139        ∀ ρ : Nat → V, Sat V Δa ρ →
   140          interp V ρ aa = interp V ρ ba
   141  
   142  /-- Inference, dual success, P currency: the subject's and the
   143  type's truthfulness — bit validity included — are *conclusions*. -/
   144  @[expose] def InferClaim (μ : CheckMode) {env : Env} (m : EnvModel V env)
   145      (φ : Name → Nat) (fuel : Nat) : Prop :=
   146    ∀ {d : Nat} {e t : Expr} {Δa : List AnnotTerm},
   147      inferTypeCore μ env fuel d e = .ok t →
   148      Expr.WScoped d e → e.looseBVarsBounded 0 = true →
   149      Expr.LeavesBounded e →
   150      ∀ {ea ta : AnnotTerm},
   151        CtxOk m φ d Δa e →
   152        denoteMeta m.acval env φ d e = some ea →
   153        denoteMeta m.acval env φ d t = some ta →
   154        (∀ ρ : Nat → V, Sat V Δa ρ → WellDenotedV V ρ ea) ∧
   155          (∀ ρ : Nat → V, Sat V Δa ρ → WellDenotedV V ρ ta) ∧
   156          ∀ ρ : Nat → V, Sat V Δa ρ →
   157            interp V ρ ea ∈ˢ interp V ρ ta
   158  
   159  /-- The P-generation step. -/
   160  @[expose] def CheckStep (μ : CheckMode) (V : Type w) [SetTheory V] : Prop :=

== ConLeche/Model/Steps/Whnf.lean#L831-L832
   831  theorem whnfStep_of (_hμ : μ.verifiedChecks = true)
   832      (hin : WhnfInputs V μ) : WhnfStep μ V :=

== ConLeche/Model/Steps/DefEq.lean#L1338-L1347
  1338  theorem defEqStep_of (hμ : μ.verifiedChecks = true)
  1339      (hin : DefEqInputs μ V) : DefEqStep μ V := by
  1340    intro env m φ fuel ihwc ihw ihd _ihi
  1341    exact defeq_claims
  1342      (defeqStep_claim (hin.hex env m φ fuel) ihwc ihw (hin.hdel env m φ)
  1343        (hin.hnat env m φ fuel) (hin.hpi env m φ fuel)
  1344        (defeqStuck_claim hμ ihd (hin.hsi env m φ fuel)
  1345          (hin.hstr env m φ) (hin.hap env m) (hin.happ env m φ fuel)
  1346          (hin.heta env m φ fuel))
  1347        (hin.hspine env m φ fuel))

== ConLeche/Model/Steps/Infer.lean#L1036-L1043
  1036  theorem inferStep_of (h : InferInputs V μ)
  1037      (hsssF : ∀ {env : Env} (m : EnvModel V env) (φ : Name → Nat)
  1038        (fuel : Nat), WhnfClaim μ m φ fuel → InferClaim μ m φ fuel →
  1039        InferClaimIO μ m φ fuel → SortSemAtIOS m μ φ fuel)
  1040      (hμ : μ.verifiedChecks = true) :
  1041      InferStep μ V := by
  1042    intro env m φ fuel _hv _ihwc ihw ihd ihi ihio
  1043    have hss : SortSemAt m μ φ fuel :=

== ConLeche/Kernel/Basis/False.lean#L52
    52  def falseBasis : List ConstantInfo := [falseRaw, falseRecRaw]

== ConLeche/Kernel/Basis.lean#L68-L70
    68  def basisPinHit (block : List ConstantInfo) : Option BasisKind :=
    69    ([BasisKind.eqK, .natK, .punitK, .emptyK, .falseK].find? fun k =>
    70        k.decls.map (·.name) == block.map (·.name)).filter fun k =>

== ConLeche/Kernel/Env.lean#L614-L621
   614  def indParamsOk (nP : Nat) (block : List ConstantInfo) : Bool :=
   615    block.all fun ci => match ci with
   616      | .indInfo cvT _ =>
   617        match cvT.type.piSortTeleLen? with
   618        | some n => decide (nP ≤ n)
   619        | none => true
   620      | .ctorInfo _ nPc _ => nPc == nP
   621      | _ => true

== ConLeche/Kernel/Inductives/SumInstall.lean#L162
   162  def normPosDom (ops : CheckerOps m) (env : Env) (T : Name) : Nat → Nat → Expr → m Expr

== ConLeche/Kernel/Inductives/NativeInstall.lean#L546
   546  def classifyFixKinds (T : Name) (lps : List Name) (nP nIdx : Nat)

== ConLeche/Kernel/Inductives/NativeInstall.lean#L617
   617  def checkNative (ops : CheckerOps m) (env : Env) (p₀ : NativeParts) : m Env := do

== ConLeche/SetModel/Value.lean#L510-L517
   510  noncomputable def lfpFamSpace (w : Nat) (I : V) : V := piR (w + 1) I fun _ => univ w
   511  
   512  /-- `(I → Sort w) → (I → Sort w)`, the functor space. -/
   513  noncomputable def lfpFamFunSpace (u w : Nat) (I : V) : V :=
   514    piR (Nat.max u (w + 1)) (lfpFamSpace V w I) fun _ => lfpFamSpace V w I
   515  
   516  /-- `lfpFam.{u,w}`; result sort `max (u + 1) (w + 1)`. -/
   517  noncomputable def lfpFamV (u w : Nat) : V :=

== ConLeche/SetModel/Container.lean#L598
   598  theorem container_closed_exists {w : Nat} (hw : w ≠ 0) {I : V} (Φ : V → V)

== ConLeche/Model/Inductives/DeclNative.lean#L63
    63  theorem declNative (hμ : μ.verifiedChecks = true) {F : Nat} {env env₂ : Env}

== ConLeche/Kernel/Inductives/Modeled.lean#L68-L79
    68  def checkIotaThm (ops : CheckerOps m) (env' envSelf : Env)
    69      (f : Name → Name) (cvName : Name) (lps : List Name) (tyA : Expr)
    70      (mI rP j : Nat) (r : RecRule) (cvj : ConstantVal)
    71      (cnP cnF : Nat) (rhsA : Expr) : m Unit := do
    72      let cvt ← unwrapOr
    73          (env'.findCV? ((cvName.str "_model").str s!"iota_{j}"))
    74          (.notImplemented s!"missing iota theorem for {cvName}")
    75      unless cvt.levelParams = lps do
    76        throw (.notImplemented s!"iota theorem level mismatch for {cvName}")
    77      -- open the theorem's telescope: params, motives, minors, fields
    78      let depth := rP + cnF
    79      let (fvs, tbody) ← unwrapOr (openPisAtFvars depth cvt.type 0)

== ConLeche/Frontend/InModel/Kit.lean#L7-L15
     7  /-!
     8  # The in-process modeller's kit (task #200)
     9  
    10  Shared pieces of the in-process construction of `_model` families for
    11  nested and mutual inductive blocks (`ConLeche/Frontend/InModel/*`):
    12  
    13  * the naming scheme (lean-inductive-models' `_impl` names, so the two
    14    generators' streams are diffable — none of these names is special to
    15    the checker: only the public `_model` slots are consumed, and only

== ConLeche/Frontend/InModel/Nested.lean#L47-L54
    47  Declines (the residual): a field mentioning the block other than as a
    48  whole member/mimic carrier (an occurrence under a binder — infinitary
    49  nesting), a container that is itself nested or mutual or whose mimics
    50  form a cycle (B4), a reflexive member, a `Prop` block with a large
    51  eliminator, a container field a later container field depends on, an
    52  index domain whose sort not even a ceiling bounds (`Kit.sortCeil`,
    53  task #227).  (Members' parameter telescopes and sorts are not
    54  compared: task #218, `Mutual.lean`'s header.)

== ConLeche/Kernel/Core.lean#L582-L590
   582  /-- The certified structural-`Nat` operations.  Six of them
   583  (`add sub mul pow beq ble`) carry a literal fast path; `Nat.pred` is
   584  here without one — it has no fast path (official's `reduce_nat` folds
   585  nothing unary but `Nat.succ`), but `Nat.sub`'s recurrence
   586  `sub x (succ y) = pred (sub x y)` names it, so its own recurrences
   587  must be certified for `sub`'s literal fold to be sound. -/
   588  def natOpNames : List Name :=
   589    [natPredName, natAddName, natSubName, natMulName, natPowName,
   590     natBeqName, natBleName]

== ConLeche/Kernel/Core.lean#L514-L531
   514  /-! ## Structural-Nat literal acceleration
   515  
   516  The official kernel accelerates the structural `Nat` operations on
   517  literals (GMP-backed there).  Here the fast path is *certified*: an
   518  operation participates only when its defining recurrence equations
   519  hold by definitional equality — checked once, at install: `checkDecl`
   520  positively rejects a nonstandard definition under one of these names,
   521  so *presence in the store is the certificate* (no runtime flag, no
   522  re-checking; a reduction-time re-check would in fact livelock — the
   523  certification's own `pred zero` equation re-enters the fast path).
   524  
   525  The certification runs in the environment *before* the operation is
   526  stored, on the equations with the operation's self-references replaced
   527  by its (annotated) definition value (`Expr.substConst0`): running it
   528  after insertion would let the operation's own just-enabled fast path
   529  discharge its all-literal-argument equations (`pred zero ≡ zero`,
   530  `beq zero zero ≡ true`) vacuously — accepting definitions that
   531  disagree with the fast path on those points, which is unsound.  The

== ConLeche/Kernel/Checker.lean#L110-L117
   110  def certifyNatEqs (ops : CheckerOps m) (env : Env) :
   111      List (Expr × Expr) → m Bool
   112    | [] => pure true
   113    | eq :: rest => do
   114      if ← ops.isDefEq env 2 eq.1 eq.2 then
   115        certifyNatEqs ops env rest
   116      else pure false
   117  

== ConLeche/Model/NatEqs.lean#L1100
  1100  theorem natOps_install (mp : EnvModelM V μ env) {φ : Name → Nat}

== ConLeche/Kernel/Core.lean#L592-L607
   592  /-- The WF-recursive operations with a *pinned-declaration* certified
   593  fast path: at install, `checkDecl` compares the stream's definition
   594  against a vendored pin of the toolchain's own (helper-unfolded)
   595  definition by definitional equality, and then checks the pinned
   596  `Nat.ble`-guarded characterization certificates
   597  (`ConLeche/Kernel/NatOpPins.lean`) like theorem declarations — without
   598  installing them.  Presence in the store is therefore again the
   599  capability: a stored operation under one of these names has passed pin
   600  and certificates, or the install declined.  (The name is historic:
   601  the family started with `Nat.div`/`Nat.mod` and now covers every
   602  pin-certified WF-recursive kernel-accelerated `Nat` operation —
   603  `Nat.log2` left the list when its fast path did, official folding no
   604  unary operation but `Nat.succ`.) -/
   605  def natDivModNames : List Name :=
   606    [natDivName, natModName, natGcdName, natLandName, natLorName,
   607     natXorName, natShiftLeftName, natShiftRightName]

== ConLeche/Kernel/NatOpPins.lean#L1-L16
     1  module
     2  public import ConLeche.Kernel.NatOpPinSet
     3  meta import ConLeche.PinGen.Dump
     4  
     5  /-!
     6  # Pinned Nat-operation declarations and certificate proofs (generated)
     7  
     8  `#load_natop_pins` below splices, per **committed dump** listed —
     9  `pins/<toolchain>.json`, the repository's top-level `pins/` directory,
    10  see `pins/README.md` — one **pin variant** `natOpPinSet_v<i> :
    11  NatOpPinSet` (`ConLeche/Kernel/NatOpPinSet.lean`): per pin-certified
    12  operation (`Nat.div`, `Nat.mod`, …) that toolchain's pinned defining
    13  expression and certificate proof blobs, as constants
    14  `nat…DeclPin_v<i> : Expr` / `nat…CertProofs_v<i> : List Expr`.  The
    15  variants are listed in `natOpPinSets` in the order below.  That list is
    16  what the shipped binary hands the fold as its PIN-LIST PARAMETER (task

== ConLeche/PinGen/Certs.lean#L7-L18
     7  /-!
     8  # Certificate theorems for the pin-certified Nat operations (elab-time)
     9  
    10  The characterization certificates of the pin-certified WF-recursive
    11  `Nat` operations, elaborated against the ambient *toolchain* prelude,
    12  where `Nat.div`/`Nat.mod`/… are the real operations.  The *statements*
    13  fix the pinned spellings: guards via the already-certified `Nat.ble`
    14  (never the `Nat.le`/`Nat.lt` `Prop` inductives), numerals via
    15  `Nat.succ`/`Nat.zero` (never `OfNat`), so the model-side consumption
    16  rides the existing `NatOpsOk` literal semantics.
    17  
    18  The proofs are written with *controlled* dependencies: no `simp`, no

== ConLeche/Model/DivModCert.lean#L2023
  2023  theorem divMod_install {F : Nat} (mp : EnvModelM V μ env)

== ConLeche/Frontend/Prepare.lean#L165-L172
   165  /-- **`preparePrelude`**: the parsed stream, prepared for the fold —
   166  the prelude's declarations first (the stream's own copies where it has
   167  them), the rest of the stream after them, every pinned `Nat`
   168  operation's stream-certified ground ahead of it.  Total, pure, and the
   169  fold's input — an array in and an array out, the shape the parse
   170  returns and the fold consumes. -/
   171  def preparePrelude (pre : PreludeIx) (ds : Array Declaration) : Array Declaration :=
   172    (prepareD pre ds).decls

== ConLeche/Frontend/NatOpGround.lean#L8-L25
     8  /-!
     9  # Hoisting a pinned `Nat` operation's stream-certified ground (task #191)
    10  
    11  The second half of the order-insensitivity fix, beside the built-in
    12  prelude (`ConLeche/Frontend/Prelude.lean`).
    13  
    14  A pin-certified operation's certificate *statements* are spelled over
    15  the structural `Nat` operations (`natOpDeps`: `Nat.shiftLeft`'s over
    16  `Nat.ble` and `Nat.sub`, `Nat.land`'s over `Nat.mul`, …), and the
    17  install guard `divModEnvGuard` requires those stored — they are what
    18  the model reads the statements through.  They are NOT in the
    19  operation's own dependency closure, so an export that orders
    20  declarations by a DFS from arbitrary roots (`lean4export` walks
    21  `env.constants` in hash order; the raw export of
    22  `tests/e2e/src/natop_order.lean` emits `Nat.shiftLeft` before
    23  `Nat.ble`/`Nat.sub`/`Bool`) declined at the install.
    24  
    25  They cannot go into the prelude: the structural operations are

== ConLeche/Verify/Frontend/Prepare.lean#L157-L162
   157  /-- **THE SPEC** (maintainer, task #293): *"it is a permutation of the
   158  input plus additional declarations, but nothing missing"* — and the
   159  additional declarations are the built-in prelude's own records. -/
   160  theorem preparePrelude_perm (pre : PreludeIx) (ds : Array Declaration) :
   161      ∃ extra : List Declaration, (∀ d ∈ extra, d ∈ pre.decls.toList) ∧
   162        (preparePrelude pre ds).toList.Perm (ds.toList ++ extra) := by

== ConLeche/SetTheory/Core.lean#L95-L156
    95  /-- A model of set theory of exactly the strength the checker needs:
    96  membership, the six ZF⁻ axioms (extensionality, pairing, union, power
    97  set, regularity, Lean-level replacement), and an ω-chain of
    98  Grothendieck universes.  Infinity is derivable; choice is inherited
    99  from the meta-logic (`Classical.choice`); see the module docstring. -/
   100  class SetTheory (V : Type u) where
   101    /-- Set membership. -/
   102    Mem : V → V → Prop
   103    /-- Extensionality: sets with the same members are equal. -/
   104    ext : ∀ {x y : V}, (∀ z, Mem z x ↔ Mem z y) → x = y
   105    /-- Pairing: the unordered pair. -/
   106    upair : V → V → V
   107    /-- Characterization of the unordered pair. -/
   108    mem_upair : ∀ {z a b : V}, Mem z (upair a b) ↔ z = a ∨ z = b
   109    /-- Union: the union of the members. -/
   110    sUnion : V → V
   111    /-- Characterization of the union. -/
   112    mem_sUnion : ∀ {z x : V}, Mem z (sUnion x) ↔ ∃ y, Mem y x ∧ Mem z y
   113    /-- Power set. -/
   114    power : V → V
   115    /-- Characterization of the power set: members are the subsets. -/
   116    mem_power : ∀ {z x : V}, Mem z (power x) ↔ ∀ w, Mem w z → Mem w x
   117    /-- Regularity: every nonempty set has an `∈`-minimal member. -/
   118    regularity : ∀ x : V, (∃ y, Mem y x) → ∃ y, Mem y x ∧ ¬ ∃ z, Mem z y ∧ Mem z x
   119    /-- Replacement, as a Lean-level scheme: the image of a set under an
   120    arbitrary function `V → V`. -/
   121    image : (V → V) → V → V
   122    /-- Characterization of the replacement image. -/
   123    mem_image : ∀ {f : V → V} {a z : V}, Mem z (image f a) ↔ ∃ w, Mem w a ∧ z = f w
   124    /-- An ω-chain of Grothendieck universes: the sets interpreting the
   125    universe tower (Carneiro's ω-many inaccessibles, op. cit.;
   126    intended model `V_{κ n}`). -/
   127    univChain : Nat → V
   128    /-- The chain increases strictly: each universe is a member of the
   129    next. -/
   130    univChain_mem : ∀ n : Nat, Mem (univChain n) (univChain (n + 1))
   131    /-- Each chain member is a Grothendieck universe (Tarski's Axiom A
   132    matrix with transitivity, `IsTGUniverse`). -/
   133    univChain_tg : ∀ n : Nat, IsTGUniverse Mem (univChain n)
   134  
   135  namespace SetTheory
   136  
   137  @[inherit_doc] scoped infix:50 " ∈ˢ " => Mem
   138  
   139  variable {V : Type u} [SetTheory V]
   140  
   141  /-- Subset, from membership. -/
   142  protected def Subset (x y : V) : Prop := ∀ z, z ∈ˢ x → z ∈ˢ y
   143  
   144  @[inherit_doc] scoped infix:50 " ⊆ˢ " => SetTheory.Subset
   145  
   146  theorem Subset.refl (x : V) : x ⊆ˢ x := fun _ hz => hz
   147  
   148  theorem Subset.trans {x y z : V} (h₁ : x ⊆ˢ y) (h₂ : y ⊆ˢ z) : x ⊆ˢ z :=
   149    fun w hw => h₂ w (h₁ w hw)
   150  
   151  theorem Subset.antisymm {x y : V} (h₁ : x ⊆ˢ y) (h₂ : y ⊆ˢ x) : x = y :=
   152    ext fun z => ⟨h₁ z, h₂ z⟩
   153  
   154  theorem mem_power_iff_subset {z x : V} : z ∈ˢ power x ↔ z ⊆ˢ x := mem_power
   155  
   156  end SetTheory

== ConLeche/SetTheory/Core.lean#L89
    89  def IsTGUniverse {V : Type u} (mem : V → V → Prop) (u : V) : Prop :=

== bridge/lean4lean-model/ConLecheBridge/Carneiro.lean#L200-L202
   200  theorem carneiro_implies_conleche :
   201      OmegaInaccessibles.{u} → Nonempty (Σ V : Type (u + 1), ConLeche.SetTheory V) :=
   202    fun h => ⟨⟨ZFSet.{u}, setTheoryOfCarneiro h⟩⟩

== ConLeche/Kernel/StdAxioms.lean#L38-L42
    38  /-- The name `propext`. -/
    39  def propextName : Name := anonymous |>.str "propext"
    40  
    41  /-- The name `Classical.choice`. -/
    42  def choiceName : Name := (anonymous |>.str "Classical") |>.str "choice"

== ConLeche/Kernel/TrustAxioms.lean#L10-L38
    10  /-!
    11  # The compiler-trust axiom family (task #95)
    12  
    13  `Init`'s compiler-trust scaffolding is installed rather than
    14  declined:
    15  
    16  * `Lean.trustCompiler : True` is trivially realizable — it is
    17    installed as an *opaque* (stored `thmInfo`, exactly like a checked
    18    `opaque` declaration) with value `True.intro`, over a pinned `True`
    19    family.  No new meta-axiom: the model is `True.intro`'s own
    20    interpretation.
    21  * `Lean.reduceNat` / `Lean.reduceBool` then check as ordinary
    22    `opaque`s (their exported values are identity functions modulo the
    23    `have := trustCompiler` wrapper); at install their stored values are
    24    pinned against the identity function
    25    (`ConLeche/Kernel/TrustPins.lean`, hand-pinned since task #273 —
    26    every toolchain's definition after zeta) by definitional equality —
    27    the task-#47 pattern: drift declines, never silently.
    28  * `Lean.ofReduceNat` / `Lean.ofReduceBool` are pinned axioms over
    29    those stored opaques (the task-#34 standard-axioms machinery).  With
    30    the stored reduce operation certified to be the identity (a
    31    definitional-equality certificate at the axiom's own install,
    32    `checkOfReduceAx`), `∀ a b, reduceNat a = b → a = b` interprets to
    33    an inhabited proposition (the hypothesis *is* the conclusion), so
    34    both axioms are true in the set model with the proof point as value.
    35  
    36  `sorryAx` remains the only axiom tolerated as a *declaration*: its
    37  record installs nothing and any use of it declines
    38  (`sorryAxName`, `ConLeche/Kernel/Basis/Names.lean`).

>>> README.md

== ConLeche/Cached/ParsedC.lean#L226-L227
   226        else if cvA.name = sorryAxName then
   227          pure fe

== Main.lean#L747
   747    "  --jobs=<n>        the number of worker threads for the check phase",

== ConLeche/Kernel/Basis/False.lean#L51-L52
    51  /-- The pinned `False` basis block, in install order. -/
    52  def falseBasis : List ConstantInfo := [falseRaw, falseRecRaw]

== Main.lean#L992
   992  def main (args : List String) : IO UInt32 := do

== ConLeche/MainTheorem.lean#L110-L117
   110  theorem no_False_declaration (V : Type w) [SetTheory V]
   111      (pins : List NatOpPinSet) (chunks : List ByteArray)
   112      (h : jsonWithTheoremFalse chunks) :
   113      ∃ e, (do
   114        let pre ← builtinPreludeE
   115        let r ← parseChunks chunks
   116        let ds := preparePrelude pre r.decls
   117        checkDecls .verified pins ds) = .error e := by

== ConLeche/Denotes.lean#L270-L290
   270  structure Model (V : Type w) [SetTheory V] (env : Env) where
   271    /-- the set a constant denotes, per level assignment -/
   272    cval : Name → (LevelParam → Nat) → V
   273    /-- every stored constant is a member of what its type denotes, at
   274    every level assignment (and every variable environment — the type
   275    is closed) -/
   276    mem : ∀ c ∈ env.consts, ∀ (φ : LevelParam → Nat) (ρ : BVarIdx → V),
   277      ∃ T, Denotes cval env φ ρ c.toConstantVal.type T ∧ cval c.name φ ∈ˢ T
   278    /-- whatever the built-in `False` denotes is the empty set -/
   279    false_empty : ∀ (φ : LevelParam → Nat) (ρ : BVarIdx → V) (F : V),
   280      Denotes cval env φ ρ (.const falseName []) F → F = empty
   281    /-- whatever the built-in `Eq` denotes is set equality: at a type `A`
   282    of the universe the level `u` names, and two of its members `a` and
   283    `b`, `@Eq.{u} A a b` denotes the truth value of `a = b`.  The three
   284    membership premises are the graph's domains — `Eq`'s denotation is a
   285    three-fold graph over `univ (Level.eval φ u)`, then over `A`, then
   286    over `A` again, and a graph read off its domain says nothing -/
   287    eq_equality : ∀ (u : Level) (φ : LevelParam → Nat) (ρ : BVarIdx → V) (E A a b : V),
   288      Denotes cval env φ ρ (.const eqName [u]) E →
   289      A ∈ˢ univ (Level.eval φ u) → a ∈ˢ A → b ∈ˢ A →
   290      app (app (app E A) a) b = eqv a b

== ConLeche/Denotes.lean#L134-L135
   134  inductive Denotes (cval : Name → (LevelParam → Nat) → V) (env : Env) (φ : LevelParam → Nat) :
   135      (BVarIdx → V) → Expr → V → Prop

== ConLeche/Cached/Installed.lean#L450-L455
   450  def checkDecls (mode : CheckMode) (pins : List NatOpPinSet)
   451      (ds : Array Declaration) :
   452      Except (CheckError × Nat) Env := do
   453    let (p, _) ← (ds.foldlM (annotDeclStep mode pins) (0, mkFEnv Env.empty, #[])) {}
   454    checkPendingList mode p.2.1 p.2.2.toList
   455    pure p.2.1.env

== ConLeche/Frontend/Prepare.lean#L165-L172
   165  /-- **`preparePrelude`**: the parsed stream, prepared for the fold —
   166  the prelude's declarations first (the stream's own copies where it has
   167  them), the rest of the stream after them, every pinned `Nat`
   168  operation's stream-certified ground ahead of it.  Total, pure, and the
   169  fold's input — an array in and an array out, the shape the parse
   170  returns and the fold consumes. -/
   171  def preparePrelude (pre : PreludeIx) (ds : Array Declaration) : Array Declaration :=
   172    (prepareD pre ds).decls

== ConLeche/Verify/Frontend/Prepare.lean#L157-L162
   157  /-- **THE SPEC** (maintainer, task #293): *"it is a permutation of the
   158  input plus additional declarations, but nothing missing"* — and the
   159  additional declarations are the built-in prelude's own records. -/
   160  theorem preparePrelude_perm (pre : PreludeIx) (ds : Array Declaration) :
   161      ∃ extra : List Declaration, (∀ d ∈ extra, d ∈ pre.decls.toList) ∧
   162        (preparePrelude pre ds).toList.Perm (ds.toList ++ extra) := by

== ConLeche/Verify/Cached/StreamConsts.lean#L781-L786
   781  theorem checkDecls_consts (V : Type w) [SetTheory V]
   782      {ds : Array Declaration} {env : Env} (accepted : checkDecls .verified pins ds = .ok env)
   783      {pd : Declaration} (hmem : pd ∈ ds) {cv : ConstantVal} (hcv : Declaration.Declares pd cv) :
   784      ∃ c, env.find? cv.name = some c ∧
   785        c.toConstantVal.levelParams = cv.levelParams ∧
   786        AnnotOf cv.type c.toConstantVal.type := by

== ConLeche/SetTheory/Core.lean#L95-L133
    95  /-- A model of set theory of exactly the strength the checker needs:
    96  membership, the six ZF⁻ axioms (extensionality, pairing, union, power
    97  set, regularity, Lean-level replacement), and an ω-chain of
    98  Grothendieck universes.  Infinity is derivable; choice is inherited
    99  from the meta-logic (`Classical.choice`); see the module docstring. -/
   100  class SetTheory (V : Type u) where
   101    /-- Set membership. -/
   102    Mem : V → V → Prop
   103    /-- Extensionality: sets with the same members are equal. -/
   104    ext : ∀ {x y : V}, (∀ z, Mem z x ↔ Mem z y) → x = y
   105    /-- Pairing: the unordered pair. -/
   106    upair : V → V → V
   107    /-- Characterization of the unordered pair. -/
   108    mem_upair : ∀ {z a b : V}, Mem z (upair a b) ↔ z = a ∨ z = b
   109    /-- Union: the union of the members. -/
   110    sUnion : V → V
   111    /-- Characterization of the union. -/
   112    mem_sUnion : ∀ {z x : V}, Mem z (sUnion x) ↔ ∃ y, Mem y x ∧ Mem z y
   113    /-- Power set. -/
   114    power : V → V
   115    /-- Characterization of the power set: members are the subsets. -/
   116    mem_power : ∀ {z x : V}, Mem z (power x) ↔ ∀ w, Mem w z → Mem w x
   117    /-- Regularity: every nonempty set has an `∈`-minimal member. -/
   118    regularity : ∀ x : V, (∃ y, Mem y x) → ∃ y, Mem y x ∧ ¬ ∃ z, Mem z y ∧ Mem z x
   119    /-- Replacement, as a Lean-level scheme: the image of a set under an
   120    arbitrary function `V → V`. -/
   121    image : (V → V) → V → V
   122    /-- Characterization of the replacement image. -/
   123    mem_image : ∀ {f : V → V} {a z : V}, Mem z (image f a) ↔ ∃ w, Mem w a ∧ z = f w
   124    /-- An ω-chain of Grothendieck universes: the sets interpreting the
   125    universe tower (Carneiro's ω-many inaccessibles, op. cit.;
   126    intended model `V_{κ n}`). -/
   127    univChain : Nat → V
   128    /-- The chain increases strictly: each universe is a member of the
   129    next. -/
   130    univChain_mem : ∀ n : Nat, Mem (univChain n) (univChain (n + 1))
   131    /-- Each chain member is a Grothendieck universe (Tarski's Axiom A
   132    matrix with transitivity, `IsTGUniverse`). -/
   133    univChain_tg : ∀ n : Nat, IsTGUniverse Mem (univChain n)

== bridge/lean4lean-model/ConLecheBridge/Carneiro.lean#L200-L202
   200  theorem carneiro_implies_conleche :
   201      OmegaInaccessibles.{u} → Nonempty (Σ V : Type (u + 1), ConLeche.SetTheory V) :=
   202    fun h => ⟨⟨ZFSet.{u}, setTheoryOfCarneiro h⟩⟩

== ConLeche/Kernel/PropWhen.lean#L413-L415
   413  structure PropWhen where
   414    private ofRepr ::
   415    private repr : PropWhenRepr

== Main.lean#L730
   730    "  --trusted         the unverified mode: the SAME checker bodies as",

