# Trusted-mode-specific expected exit codes, read by tests/arena.sh's
# trusted sweep (task #147; successor of tests/no-model-expected.txt,
# renamed with the mode at the 2026-09-06 mode rename, itself the
# successor of tests/yolo-expected.txt, task #139).  Line format:
#
#   <exit-code> <suite> <fixture> [mode]
#
# with suite ∈ {arena, e2e}, and <fixture>/[mode] spelled exactly as in
# tests/arena-expected.txt resp. tests/e2e-expected.txt.  A fixture with
# no line here is expected to give, under `--trusted`, the same exit
# code the default (`--verified`) mode gives.
#
# What may be recorded here: divergences that are the trusted lane's
# NATURE, each with a comment naming the mechanism.  The lane
# (since 2026-09-06 the one cached driver at `.trusted`, `ConLeche/Cached/
# ParsedC.lean` + `CoreC.lean`) runs a full checking-mode front door —
# so the old yolo mode's class-B argument-position escapes are GONE:
# the three class-B witnesses of task #139 (yolo_arg_escape,
# yolo_arg_escape_univs, yolo_decline_vs_accept) are all caught at the
# front door again and need no accept-override.  What remains is the
# error-CLASS difference: with the certificate families off, a defect
# the certified stack positively detects as an unsupported feature
# (decline, exit 2) inside a certificate-only code path may instead
# surface as a plain type mismatch (reject, exit 1), or vice versa.
# A 1-vs-2 pair like that is the mode's nature and gets a line here.
#
# NOTHING ELSE belongs here.  A good stream rejected, or a bad stream
# accepted, under --trusted is a bug in the cert-skipping twins
# (task #139's drift lesson): fix the twin, do not record the flip.

# (Task #161 P5: the yolo_decline_vs_accept override is GONE — the
# certified lane now rejects that fixture too, for its own reason; see
# tests/e2e-expected.txt.  Both lanes agree, so nothing is recorded.)

# The annotated fixture suite's decline fixtures (task #161, suite
# `annot`, tests/annot-expected.txt): the verified mode validates the
# "pw" sort annotations and positively declines a mismatched claim;
# the trusted lane writes the same annotations but never VALIDATES them
# (validation is certification-only work), so the underlying
# well-typed declarations accept.  The lane's nature, one line per
# decline fixture.
0 annot annot_decline_forall.ndjson
0 annot annot_decline_lam_chain.ndjson
0 annot annot_decline_lam_leaf.ndjson
